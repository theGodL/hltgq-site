version: "3.8"

services:
  hltgq-site:
    image: hltgq-site:latest
    container_name: hltgq-site
    restart: unless-stopped
    ports:
      - "18687:8080"
    environment:
      - TZ=Asia/Shanghai
    # 持久化：日志 + 决策 Excel 快照（容器重建不丢，下载按 recordId 取用防串档）
    volumes:
      - ./logs:/app/logs
      - ./decision-excel:/app/decision-excel
    # 资源限制（按服务器配置调整）
    deploy:
      resources:
        limits:
          memory: 1024M
        reservations:
          memory: 512M
    # 健康检查
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
