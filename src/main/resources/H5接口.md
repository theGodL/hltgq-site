# H5 移动端接口文档

> 面向 H5 移动端（巡查统计 / 维修养护统计 / 消息中心），生产环境路径统一加 `/hltgq-site` 前缀
> 全部接口需登录态（未登录由拦截器统一 302/401 处理）
> 数据来源为低代码平台表（schema `qixiao-apaas`）

---

## 设计说明

1. **消息可见性口径**：消息按接收规则（部门/岗位/角色/人员）决定可见范围，只有匹配规则的登录人能看到；某类型未配置规则时全员可见。接收记录由后端在 summary/page 接口被调用时按需同步补建（幂等、不重复），无定时分发；规则收紧时存量接收记录不回滚（已读状态保留）。
2. **已读口径**：接收记录建立时为未读（`is_read=#1#`），登录人阅读后更新为已读（`#2#`）；列表与未读数均基于当前登录人视角返回。
3. **统计口径**：柱状图巡查数按巡检计划统计（排除草稿 `#1#`、含已取消 `#4#`，按计划开始时间 `start_time` 归月）；维修养护饼图仅统计已提交巡检记录（`status=#2#`）；问题记录全状态、按发现时间 `time` 统计。
4. **图表展示口径（配色、堆叠/并列、轴标题）归前端**，后端只返回权威数据（编码 + 数量 + 完整编码映射表）。

---

## 一、巡查及问题统计（柱状图）

近 12 个月每月巡查任务数（巡检计划口径，含已取消）与问题数量；问题数按风险等级拆分三档，前端按不同颜色绘制（堆叠或并列由前端定夺）。

### 请求

```
GET /h5/patrol-issue-monthly
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| endMonth | String | 否 | 当前月 | 统计基准月，格式 `yyyy-MM`；返回该月及往前 11 个月（共 12 个） |

### 调用示例

```
/hltgq-site/h5/patrol-issue-monthly
/hltgq-site/h5/patrol-issue-monthly?endMonth=2026-09
```

### 响应

```json
{
  "months": [
    { "month": "2025-10", "patrolCount": 12, "issueCount": 5, "issueLow": 3, "issueMid": 1, "issueHigh": 1 },
    { "month": "2025-11", "patrolCount": 9,  "issueCount": 2, "issueLow": 2, "issueMid": 0, "issueHigh": 0 },
    { "month": "2025-12", "patrolCount": 0,  "issueCount": 0, "issueLow": 0, "issueMid": 0, "issueHigh": 0 }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| months | Array | 严格 12 个元素，按月份升序（endMonth 往前 11 个月起），无数据月份补 0，前端 x 轴直接消费 |

**months[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| month | String | 月份标签 `yyyy-MM` |
| patrolCount | long | 该月巡查任务数（巡检计划数：排除草稿 `#1#`、含已取消 `#4#`） |
| issueCount | long | 该月问题总数（全状态，含未标注风险等级的记录） |
| issueLow | long | 低风险（`risk_level=#1#`）问题数 |
| issueMid | long | 中风险（`risk_level=#2#`）问题数 |
| issueHigh | long | 高风险（`risk_level=#3#`）问题数 |

### 计算规则

```
1. 基准月 M = endMonth（默认当前月），窗口 = [M−11个月首日, M+1个月首日)（右开区间，避免月末 23:59:59 精度问题）
2. 巡查：t_auto_hltgq_water_patrol_schedule（巡检计划），status IN ('#2#','#3#','#4#')（排除草稿 #1#、含已取消 #4#），按 start_time（计划开始时间）聚合到月
3. 问题：t_auto_hltgq_knc3g_bpzjoh，全状态，按 time（发现时间）聚合到月，
   并按 risk_level 拆 #1#/#2#/#3# 三档
4. 生成 12 个月完整序列，无数据月份补 0
```

### 边界场景

| 场景 | 表现 |
|------|------|
| 某月无任何记录 | 四项计数均为 0 |
| 问题 risk_level 为 null/未知编码 | 计入 issueCount，不计入 issueLow/Mid/High（三档之和可能小于 issueCount） |
| 巡检计划草稿（status=#1#） | 不计入 patrolCount |
| 已取消计划（status=#4#） | 计入 patrolCount（业务口径：取消的计划仍属巡查任务） |

---

## 二、维修养护统计（饼状图）

已提交巡检记录按巡检结果 `result` 分组计数，固定返回 6 档。

### 请求

```
GET /h5/inspection-result-stats
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| startTime | String | 否 | 不限 | 巡检时间起（含）`yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | 不限 | 巡检时间止（含）`yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/h5/inspection-result-stats
/hltgq-site/h5/inspection-result-stats?startTime=2026-01-01+00:00:00&endTime=2026-12-31+23:59:59
```

### 响应

```json
{
  "total": 100,
  "items": [
    { "name": "#1#", "label": "待填写", "value": 2 },
    { "name": "#2#", "label": "正常",   "value": 60 },
    { "name": "#3#", "label": "异常",   "value": 10 },
    { "name": "#4#", "label": "隐患",   "value": 8 },
    { "name": "#5#", "label": "缺陷",   "value": 5 },
    { "name": "#6#", "label": "故障",   "value": 15 }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 已提交巡检记录总数 |
| items | Array | 固定 6 档（#1#~#6#），无数据的档 value=0 |

**items[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| name | String | 巡检结果编码 |
| label | String | 巡检结果名称（后端返回权威映射） |
| value | long | 该结果的巡检记录数 |

### 计算规则

```
1. 巡检：t_auto_hltgq_water_inspection_record，status='#2#'（已提交，排除草稿）
2. 按 result 分组计数：#1# 待填写、#2# 正常、#3# 异常、#4# 隐患、#5# 缺陷、#6# 故障
3. 缺档补 0，固定按编码升序返回
```

---

## 三、消息中心

消息由三类数据源组成：**告警**（已确认、处理中）、**举报投诉**、**意见征集**。
告警本质是“认领”机制：平台侧把告警确认（`status` 从 `#1#` 未确认变为 `#2#` 已确认/`#3#` 处理中）后才进入消息中心，
未确认/已关闭的告警不产生消息。已读状态按当前登录人基于消息接收表判定。

### 3.1 未读消息数汇总（角标）

#### 请求

```
GET /h5/message/summary
```

#### 调用示例

```
/hltgq-site/h5/message/summary
```

#### 响应

**进入首页时调用本接口**：后端先按接收规则把当前登录人可见的消息同步补建为未读接收记录
（幂等，绝不重复、不回滚已读状态，30 秒防抖），再统计未读数，保证红点实时准确。
未读数基于消息接收表按当前登录人统计（`is_read=#1#`）。

```json
{
  "alertUnread": 3,
  "complaintUnread": 1,
  "suggestionUnread": 0,
  "totalUnread": 4
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| alertUnread | long | 未读告警数（当前登录人接收记录中未读的已确认/处理中告警） |
| complaintUnread | long | 未读举报投诉数（当前登录人接收记录中未读的） |
| suggestionUnread | long | 未读意见征集数（当前登录人接收记录中未读的） |
| totalUnread | long | 三者之和 |

### 3.2 消息分页列表

#### 请求

```
GET /h5/message/page
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| messageType | String | **是** | — | 消息类型：`#1#` 告警、`#2#` 举报投诉、`#3#` 意见征集（URL 中 `#` 需转义为 `%23`） |
| page | long | 否 | 1 | 页码 |
| size | long | 否 | 10 | 每页条数 |

#### 调用示例

```
/hltgq-site/h5/message/page?messageType=%231%23&page=1&size=10
/hltgq-site/h5/message/page?messageType=%232%23&page=1&size=10
/hltgq-site/h5/message/page?messageType=%233%23&page=1&size=10
```

#### 响应（messageType=#1#，告警）

```json
{
  "total": 8,
  "current": 1,
  "size": 10,
  "pages": 1,
  "records": [
    {
      "messageId": "alert-uuid-001",
      "code": "AL-20260908-001",
      "content": "某某站水位超限",
      "level": "#2#",
      "status": "#2#",
      "type": "#1#",
      "time": "2026-09-08T09:00:00",
      "siteId": "site-uuid-001",
      "siteName": "某某水位站",
      "deviceId": "device-uuid-001",
      "deviceName": "某某水位计",
      "isRead": false
    }
  ]
}
```

**records[].字段（告警）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| messageId | String | 消息主键（告警表 id） |
| code | String | 告警编号 |
| content | String | 告警内容 |
| level | String | 告警级别（原始编码，前端按需展示） |
| status | String | 告警状态编码：`#2#` 已确认、`#3#` 处理中 |
| type | String | 告警类型：`#1#` 阈值超限、其他/空 = 异常告警 |
| time | String | 发生时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| siteId | String | 站点 ID（告警表 site = 站点表主键） |
| siteName | String | 站点名称（LEFT JOIN 站点表，被删为 null） |
| deviceId | String | 设备 ID（告警表 device = 设备表主键） |
| deviceName | String | 设备名称（LEFT JOIN 设备表，被删为 null） |
| isRead | Boolean | 当前登录人是否已读 |

#### 响应（messageType=#2#，举报投诉）

```json
{
  "total": 5,
  "current": 1,
  "size": 10,
  "pages": 1,
  "records": [
    {
      "messageId": "complaint-uuid-001",
      "complaintType": "#1#",
      "complaintTypeLabel": "流域内违法违规行为",
      "content": "某渠段存在倾倒垃圾行为",
      "contact": "张三",
      "phone": "13800000000",
      "createdAt": "2026-09-07T14:30:00",
      "isRead": true
    }
  ]
}
```

**records[].字段（举报投诉）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| messageId | String | 消息主键（投诉表 id） |
| complaintType | String | 投诉类型编码（表字段 jllmfa） |
| complaintTypeLabel | String | 投诉类型名称（后端权威映射，见附录） |
| content | String | 投诉内容（表字段 humzvp） |
| contact | String | 联系人姓名（表字段 eoitvf） |
| phone | String | 联系方式（表字段 sgloiw） |
| createdAt | String | 提交时间（低代码平台公共字段 created_at，ISO 格式） |
| isRead | Boolean | 当前登录人是否已读 |

#### 响应（messageType=#3#，意见征集）

```json
{
  "total": 3,
  "current": 1,
  "size": 10,
  "pages": 1,
  "records": [
    {
      "messageId": "suggestion-uuid-001",
      "suggestionType": "#1#",
      "suggestionTypeLabel": "水资源管理",
      "content": "建议加强渠道巡查频次",
      "contact": "李四",
      "phone": "13900000000",
      "createdAt": "2026-09-06T10:15:00",
      "isRead": false
    }
  ]
}
```

**records[].字段（意见征集）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| messageId | String | 消息主键（意见表 id） |
| suggestionType | String | 意见类型编码（表字段 ywitii） |
| suggestionTypeLabel | String | 意见类型名称（后端权威映射，见附录） |
| content | String | 意见内容（表字段 iiyomw） |
| contact | String | 联系人姓名（表字段 ymkxgm） |
| phone | String | 联系方式（表字段 nxziie） |
| createdAt | String | 提交时间（低代码平台公共字段 created_at，ISO 格式） |
| isRead | Boolean | 当前登录人是否已读 |

#### 列表通用规则

- 三类列表均按时间倒序（告警按 `time`、投诉/意见按 `created_at`，id 降序兑底分页稳定）；
- 告警列表口径：`status IN ('#2#', '#3#')`（仅已确认、处理中，未确认/已关闭不进消息中心），无时间窗口限制（全部活跃告警）；
- 列表只返回当前登录人的接收记录（按部门/岗位/角色/人员规则决定可见范围）；page 接口内部同样先按需同步再查询（深链接直达列表数据也正确）；`isRead` = 接收记录 `is_read=#2#`；
- 无数据时返回 `total=0`、`pages=0`、`records=[]`。

### 3.3 标记已读（支持批量）

#### 请求

```
POST /h5/message/read
Content-Type: application/json
```

```json
{
  "messageType": "#1#",
  "messageIds": ["alert-uuid-001", "alert-uuid-002"]
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| messageType | String | **是** | 消息类型：`#1#` 告警 / `#2#` 举报投诉 / `#3#` 意见征集 |
| messageIds | Array\<String\> | **是** | 消息主键数组（可单条，单次最多 200 条，超出请用 3.4 全部已读） |

#### 响应

```json
{
  "success": true
}
```

幂等：重复标记已读仅更新同一条接收记录（一人对一条消息只有一条接收记录，重复调用无副作用）。

### 3.4 全部标记已读

#### 请求

```
POST /h5/message/read-all
Content-Type: application/json
```

```json
{
  "messageType": "#1#"
}
```

将当前登录人在该类型下的全部现存消息标记为已读，响应同 3.3。

---

## 附录：编码映射表

### 消息类型 message_type

| 编码 | 名称 |
|------|------|
| #1# | 告警 |
| #2# | 举报投诉 |
| #3# | 意见征集 |

### 巡检结果 result

| 编码 | 名称 |
|------|------|
| #1# | 待填写 |
| #2# | 正常 |
| #3# | 异常 |
| #4# | 隐患 |
| #5# | 缺陷 |
| #6# | 故障 |

### 问题风险等级 risk_level

| 编码 | 名称 |
|------|------|
| #1# | 低风险 |
| #2# | 中风险 |
| #3# | 高风险 |

### 告警状态 status

| 编码 | 名称 |
|------|------|
| #1# | 未确认 |
| #2# | 已确认 |
| #3# | 处理中 |
| #4# | 已关闭 |

### 投诉类型 jllmfa

| 编码 | 名称 |
|------|------|
| #1# | 流域内违法违规行为 |
| #jsud# | 业务办理态度问题 |
| #cvky# | 不文明现象 |
| #mnuu# | 其他 |

### 意见类型 ywitii

| 编码 | 名称 |
|------|------|
| #1# | 水资源管理 |
| #eosz# | 防洪减灾 |
| #olcg# | 水生态保护 |
| #avxv# | 公共服务 |
| #douo# | 其他建议 |
