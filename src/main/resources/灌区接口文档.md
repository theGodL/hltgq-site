# 灌区监测接口文档

> 面向前端，生产环境路径统一加 `/hltgq-site` 前缀
> 涵盖灌区雨量、灌区水位、水库雨情、站点综合指标、水库水情、闸门监测、流量监测、墒情监测、登录鉴权、流量计算参数、站点信息维护等模块

---

## 一、灌区雨量分页

每站点仅返回最新一条，含 1h / 3h / 6h 三档降雨增量。

### 请求

```
GET /st-pptn-r/gq-rainfall
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| page | long | 否 | 1 | 页码 |
| size | long | 否 | 20 | 每页条数 |
| stcd | String | 否 | — | 站点编号（筛选单个站点） |
| startTime | String | 否 | — | 监测起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | — | 监测截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/st-pptn-r/gq-rainfall?page=1&size=20
/hltgq-site/st-pptn-r/gq-rainfall?stcd=9000000137&startTime=2026-07-20+08:00:00&endTime=2026-07-30+07:59:59
```

### 响应

```json
{
  "total": 15,
  "current": 1,
  "size": 20,
  "pages": 1,
  "records": [
    {
      "stcd": "9000000137",
      "id": "uuid-xxx",
      "stnm": "某某雨量站",
      "tm": "2026-07-29T10:00:00",
      "drp": 125.5,
      "dyp": 28.3,
      "rain1h": 2.3,
      "rain3h": 5.1,
      "rain6h": 8.7,
      "isOnline": true
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表 |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| id | String | 站点 UUID |
| stnm | String | 站点名称 |
| tm | String | 最新监测时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| drp | Number | 时段降雨量 (mm) |
| dyp | Number | 日降雨量 (mm) |
| rain1h | Number | 近 1 小时降雨量 (mm)。`null` 仅当 DRP 为 null；无历史记录时返回当前 DRP 值 |
| rain3h | Number | 近 3 小时降雨量 (mm)。`null` 仅当 DRP 为 null；无历史记录时返回当前 DRP 值 |
| rain6h | Number | 近 6 小时降雨量 (mm)。`null` 仅当 DRP 为 null；无历史记录时返回当前 DRP 值 |
| isOnline | Boolean | 在线状态：`true` 在线 / `false` 断联。非水库站按最新采集时间断联判定（MQTT 站 30 分钟、RabbitMQ 站 70 分钟无更新判离线）；水库站按站点表 zebpsu 状态（`#1#` 在线 / `#2#` 离线）；无监测时间或状态未知默认在线 |

### 计算规则

```
rainNh = max(0, 当前DRP − N小时前最后一条DRP)
```

- 传感器归零（DRP 骤降）时增量自动取 0
- 无 N 小时前历史记录时，取当前 DRP 作为增量（SQL 使用 `COALESCE(..., 0)` 兜底，prev 默认 0）
- 当前 DRP 为 `null` 时，增量返回 `null`

---

## 二、灌区雨量变化图表

单站点小时级增量 + 累计雨量，**前端双 Y 轴图表直接消费**。

### 请求

```
GET /st-pptn-r/gq-rainfall-chart
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| stcd | String | **是** | — | 站点编号 |
| startTime | String | 否 | 7 天前整点 | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | 当前整点小时 | 截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/st-pptn-r/gq-rainfall-chart?stcd=9000000137
/hltgq-site/st-pptn-r/gq-rainfall-chart?stcd=9000000137&startTime=2026-07-20+08:00:00&endTime=2026-07-25+08:00:00
```

### 响应

```json
{
  "stcd": "9000000137",
  "stnm": "某某雨量站",
  "startTime": "2026-07-22T08:00:00",
  "endTime": "2026-07-29T08:00:00",
  "hours": [
    { "hour": "2026-07-22 08:00", "rainfall": 0.0, "cumulative": 0.0 },
    { "hour": "2026-07-22 09:00", "rainfall": 2.5, "cumulative": 2.5 },
    { "hour": "2026-07-22 10:00", "rainfall": 3.1, "cumulative": 5.6 },
    { "hour": "2026-07-22 11:00", "rainfall": 0.0, "cumulative": 5.6 },
    { "hour": "2026-07-22 12:00", "rainfall": 0.8, "cumulative": 6.4 }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| startTime | String | 查询起始时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| endTime | String | 查询截止时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| hours | Array | 小时级数据点（**严格 1h 步长，无缺失**） |

**hours[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| hour | String | 小时标签 `yyyy-MM-dd HH:00` |
| rainfall | Number | 该小时内的增量雨量 (mm)。无数据时 = 0 |
| cumulative | Number | 从 startTime 到该小时的**累计**雨量 (mm) |

### 前端图表用法

```javascript
const resp = await fetch(`/hltgq-site/st-pptn-r/gq-rainfall-chart?stcd=${stcd}`);
const { hours } = await resp.json();

// ECharts 双 Y 轴
chart.setOption({
  xAxis:  { data: hours.map(h => h.hour.slice(5, 16)) },       // 只保留 "MM-DD HH:00"
  yAxis:  [
    { name: '实时雨量 (mm)' },
    { name: '累计雨量 (mm)' }
  ],
  series: [
    { name: '实时雨量', type: 'bar',  yAxisIndex: 0, data: hours.map(h => h.rainfall) },
    { name: '累计雨量', type: 'line', yAxisIndex: 1, data: hours.map(h => h.cumulative) }
  ]
});
```

### 计算规则

```
服务端内部逻辑（前端无需关心）：
1. 向前扩展 2h 查询原始 DRP 记录（确保首小时有基线对比）
2. 按时间排序后遍历：
    第一条记录  →  inc = 0
    后续记录    →  inc = max(0, cur.DRP − prev.DRP)
3. 将增量归属到小时桶（HH:00），同一小时的多次上报累加
4. 生成完整小时序列（从 startTime 到 endTime，步长固定 1h）
5. 累计 = 逐小时 rainfall 累加
```

### 边界场景

| 场景 | 表现 |
|------|------|
| 某小时无设备上报 | `rainfall = 0`，`cumulative` 保持上小时值 |
| 传感器归零（DRP 骤降） | 归零时增量 = 0，归零后继续正常计算 |
| 整段时间无数据 | 返回空 `hours: []` |
| 数据断档后恢复 | 恢复后第一条 inc=0，后续恢复累加 |

---

## 三、灌区水位分页

每站点仅返回最新一条，含 1h 水位涨幅。

### 请求

```
GET /irrigation-water-level/page
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 20 | 每页条数 |
| stcd | String | 否 | — | 站点编号（筛选单个站点） |
| startTime | String | 否 | — | 监测起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | — | 监测截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/irrigation-water-level/page?page=1&size=20
/hltgq-site/irrigation-water-level/page?stcd=9000000137&startTime=2026-07-20+08:00:00&endTime=2026-07-30+07:59:59
```

### 响应

```json
{
  "total": 8,
  "current": 1,
  "size": 20,
  "pages": 1,
  "records": [
    {
      "stcd": "9000000137",
      "stnm": "某某水位站",
      "id": "uuid-xxx",
      "tm": "2026-07-29T10:30:00",
      "z": 12.35,
      "rise1h": 1.2,
      "isOnline": true
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表 |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| id | String | 站点 UUID |
| tm | String | 最新监测时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| z | Number | 水位值 (m) |
| rise1h | Number | 近 1 小时水位涨跌 (cm)。正值=上涨，负值=下降。`null` 表示历史无 1h 前数据 |
| isOnline | Boolean | 在线状态：`true` 在线 / `false` 离线。水位站均为库上站点，以站点表 zebpsu 状态判断（`#1#` 在线 / `#2#` 离线）；站点表未查到状态的站点默认在线 |

---

## 四、灌区水位变化图表

单站点小时级水位值 + 水位变化（cm），**前端双 Y 轴图表直接消费**。

### 请求

```
GET /irrigation-water-level/chart
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| stcd | String | **是** | — | 站点编号 |
| startTime | String | 否 | 7 天前整点 | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | 当前整点小时 | 截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/irrigation-water-level/chart?stcd=9000000137
/hltgq-site/irrigation-water-level/chart?stcd=9000000137&startTime=2026-07-20+08:00:00&endTime=2026-07-25+08:00:00
```

### 响应

```json
{
  "stcd": "9000000137",
  "stnm": "某某水位站",
  "startTime": "2026-07-22T14:00:00",
  "endTime": "2026-07-29T14:00:00",
  "hours": [
    { "hour": "2026-07-22 14:00", "waterLevel": 12.35, "change": null },
    { "hour": "2026-07-22 15:00", "waterLevel": 12.37, "change": 2.0 },
    { "hour": "2026-07-22 16:00", "waterLevel": 12.34, "change": -3.0 },
    { "hour": "2026-07-22 17:00", "waterLevel": 12.34, "change": 0.0 },
    { "hour": "2026-07-22 18:00", "waterLevel": 12.40, "change": 6.0 }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称（无匹配数据时为 `null`） |
| startTime | String | 查询起始时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| endTime | String | 查询截止时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| hours | Array | 小时级数据点（**严格 1h 步长，无缺失**） |

**hours[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| hour | String | 小时标签 `yyyy-MM-dd HH:00` |
| waterLevel | Number | 该小时水位值 (m)。无数据时为 `null` |
| change | Number | 该小时水位变化 (cm)。正值=上涨，负值=下降。首小时为 `null` |

### 前端图表用法

```javascript
const resp = await fetch(`/hltgq-site/irrigation-water-level/chart?stcd=${stcd}`);
const { hours } = await resp.json();

// ECharts 双 Y 轴
chart.setOption({
  xAxis:  { data: hours.map(h => h.hour.slice(5, 16)) },
  yAxis: [
    { name: '水位 (m)' },
    { name: '水位变化 (cm)' }
  ],
  series: [
    { name: '水位', type: 'line', yAxisIndex: 0, data: hours.map(h => h.waterLevel) },
    { name: '水位变化', type: 'bar', yAxisIndex: 1, data: hours.map(h => h.change) }
  ]
});
```

### 计算规则

```
服务端内部逻辑（前端无需关心）：
1. 向前扩展 1h 查询原始水位记录（确保首小时有基线对比）
2. 按时间排序后，按整点小时聚合：同一小时内取最新一条水位值
3. 水位变化 (cm) = (当前小时水位 − 前一小时水位) × 100
4. 生成完整小时序列（从 startTime 到 endTime，步长固定 1h）
5. 当前小时无数据时，水位值为 null，前一小时水位值继续延续（保证后续变化计算连续）
```

### 边界场景

| 场景 | 表现 |
|------|------|
| 某小时无设备上报 | `waterLevel = null`，`change = null`，前值延续 |
| 首小时 | `change = null`（无前值可对比） |
| 整段时间无数据 | `waterLevel` 和 `change` 均为 `null`，小时序列始终完整返回 |
| 水位为 0 | 正常显示 `waterLevel = 0` |

---

### 水位趋势图表

> 单站点小时级水位曲线（无 1h 涨幅，纯水位值），默认近七天，支持按日期区间查询。用于水位监测-详情趋势图。

**请求方式**：`GET`

**请求路径**：`/irrigation-water-level/trend`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcd | **是** | String | 站点编号 |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间，默认 7 天前整点 |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间，默认当前整点 |

#### 调用示例

```
/hltgq-site/irrigation-water-level/trend?stcd=9000000137
/hltgq-site/irrigation-water-level/trend?stcd=9000000137&startTime=2026-08-10+00:00:00&endTime=2026-08-17+00:00:00
```

#### 响应格式

```json
{
  "stcd": "9000000137",
  "stnm": "某某水位站",
  "startTime": "2026-08-10T00:00:00",
  "endTime": "2026-08-17T00:00:00",
  "hours": [
    { "hour": "2026-08-16 17:00", "waterLevel": 12.35 },
    { "hour": "2026-08-16 18:00", "waterLevel": null }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称（无匹配数据时回退为 stcd） |
| startTime | String | 查询起始时间（ISO 格式） |
| endTime | String | 查询截止时间（ISO 格式） |
| hours | Array | 小时级数据点（**严格 1h 步长，无缺失**） |

**hours[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| hour | String | 小时标签 `yyyy-MM-dd HH:00` |
| waterLevel | Number | 该小时水位值 (m)，截断 2 位小数。无数据时为 `null`（前端图表断线） |

#### 计算规则

1. 查询原始水位记录，按整点小时聚合：同一小时内取最新一条水位值
2. `-9991`（设备异常）、`-999`（设备不存在）视为缺失，不参与聚合
3. 生成完整小时序列（从 startTime 到 endTime，步长固定 1h），无数据小时为 `null`

#### 与 /chart 的区别

| | `/chart` | `/trend` |
|------|:--:|:--:|
| 附加列 | 含 1h 水位变化 `change`（双 Y 轴） | 纯水位曲线（单 Y 轴） |
| 用途 | 水位变化统计分析 | 水位趋势详情图 |

---

## 五、灌区雨量历史

单站点全部历史记录（非仅最新一条），每条含 1h / 3h / 6h 降雨增量。

### 请求

```
GET /st-pptn-r/gq-rainfall-history
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| stcd | String | **是** | — | 站点编号 |
| page | long | 否 | 1 | 页码 |
| size | long | 否 | 20 | 每页条数 |
| startTime | String | 否 | — | 监测起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | — | 监测截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/st-pptn-r/gq-rainfall-history?stcd=9000000137&page=1&size=20
/hltgq-site/st-pptn-r/gq-rainfall-history?stcd=9000000137&startTime=2026-07-20+08:00:00&endTime=2026-07-30+07:59:59
```

### 响应

```json
{
  "total": 150,
  "current": 1,
  "size": 20,
  "pages": 8,
  "records": [
    {
      "stcd": "9000000137",
      "id": "uuid-xxx",
      "stnm": "某某雨量站",
      "tm": "2026-07-29T10:00:00",
      "drp": 125.5,
      "dyp": 28.3,
      "rain1h": 2.3,
      "rain3h": 5.1,
      "rain6h": 8.7
    },
    {
      "stcd": "9000000137",
      "id": "uuid-xxx",
      "stnm": "某某雨量站",
      "tm": "2026-07-29T09:55:00",
      "drp": 123.1,
      "dyp": 28.0,
      "rain1h": 1.8,
      "rain3h": 4.2,
      "rain6h": 7.3
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按监测时间倒序**，最新在前） |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| id | String | 站点 UUID |
| stnm | String | 站点名称 |
| tm | String | 监测时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |
| drp | Number | 时段降雨量 (mm) |
| dyp | Number | 日降雨量 (mm) |
| rain1h | Number | 该时刻前 1h 降雨量 (mm)。`null` 仅当 DRP 为 null；无历史记录时返回当前 DRP 值 |
| rain3h | Number | 该时刻前 3h 降雨量 (mm)。`null` 仅当 DRP 为 null；无历史记录时返回当前 DRP 值 |
| rain6h | Number | 该时刻前 6h 降雨量 (mm)。`null` 仅当 DRP 为 null；无历史记录时返回当前 DRP 值 |

### 与 gq-rainfall 的区别

| | `/gq-rainfall` | `/gq-rainfall-history` |
|------|:--:|:--:|
| 每个站点 | 仅最新 1 条 | **全部记录** |
| stcd 参数 | 可选（筛选） | **必填** |
| 排序 | 按 stcd | 按 tm **倒序**（最新在前） |
| 使用场景 | 实时概览表格 | 历史数据表格 / 详情页 |

### 计算规则

与 `/gq-rainfall` 完全一致（详见第一节）：

```
rainNh = max(0, 该时刻DRP − N小时前最后一条DRP)
```

---

### 灌区日雨情（双视角）

> 非水库站点的「实时雨情 + 日雨情」双视角接口，结构完全复用[六、水库实时雨情 / 水库日雨情](#六水库实时雨情--水库日雨情)：
> - `stations[]` → **实时雨情视角**：各站点最新观测快照
> - `days[]` → **日雨情视角**：按水文日（8:00 切分）聚合的逐日雨量透视表
>
> 与水库日雨情的唯一区别是**站点集合**：雨量表全部 STCD 排除水库 13 站（STCD + 名称双重排除）。

### 请求

```
GET /st-pptn-r/gq-daily-rainfall
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| startDate | String | 否 | 当天 | 起始日期 `yyyy-MM-dd` |
| endDate | String | 否 | 当天 | 截止日期 `yyyy-MM-dd` |

### 调用示例

```
/hltgq-site/st-pptn-r/gq-daily-rainfall
/hltgq-site/st-pptn-r/gq-daily-rainfall?startDate=2026-08-10&endDate=2026-08-16
```

### 响应

```json
{
  "stations": [
    {
      "stcd": "9000000137",
      "stnm": "桐城(车富岭)",
      "id": "uuid-xxx",
      "latestTm": "2026-08-16T10:25:00",
      "latestDrp": 15.2
    }
  ],
  "days": [
    {
      "day": "2026-08-15 08:00:00",
      "values": { "9000000137": 5.2, "9000000136": 3.1 },
      "avg": 4.15
    }
  ]
}
```

### 字段说明

与[六、水库实时雨情 / 水库日雨情](#六水库实时雨情--水库日雨情)完全一致：

- **stations[]**：`stcd` / `stnm` / `id` / `latestTm`（实际观测时间，ISO 格式）/ `latestDrp`（最新观测所在水文日的 DYP 正向增量，mm）
- **days[]**：`day`（水文日标签 `yyyy-MM-dd 08:00:00`）/ `values`（键为 stcd，值为日降雨量 mm，站点 key 完整不缺）/ `avg`（该水文日站点平均值，保留 2 位小数）

### 计算规则

与水库日雨情相同（DYP 正向增量 + 水文日 8:00 切分），仅站点集合替换为非水库站点。

---

## 六、水库实时雨情 / 水库日雨情

**同一接口，两种视角**：
- `stations[]` → **实时雨情视角**：各站点最新观测快照（`latestTm` = 实际观测时间，`latestDrp` = 当前 DRP），用于实时监控卡片
- `days[]` → **日雨情视角**：按水文日（8:00 切分）聚合的逐日雨量透视表，用于日雨情报表

12 个固定水库站点。**后端一次计算，前端直接渲染**。

### 请求

```
GET /st-pptn-r/reservoir-rainfall
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| startDate | String | 否 | 当天 | 起始日期 `yyyy-MM-dd` |
| endDate | String | 否 | 当天 | 截止日期 `yyyy-MM-dd` |

### 调用示例

```
/hltgq-site/st-pptn-r/reservoir-rainfall
/hltgq-site/st-pptn-r/reservoir-rainfall?startDate=2026-07-20&endDate=2026-07-29
```

### 响应

```json
{
  "stations": [
    {
      "stcd": "3206400001",
      "stnm": "周家河",
      "id": "uuid-xxx",
      "latestTm": "2026-07-29T10:25:00",
      "latestDrp": 125.5
    }
  ],
  "days": [
    {
      "day": "2026-07-28 08:00:00",
      "values": {
        "3206400001": 5.2,
        "3206400002": 3.1,
        "3206400005": 0.0
      },
      "avg": 0.69
    },
    {
      "day": "2026-07-29 08:00:00",
      "values": {
        "3206400001": 2.8,
        "3206400002": 0.0,
        "3206400005": 1.5
      },
      "avg": 0.36
    }
  ]
}
```

### 字段说明

**stations[] — 实时雨情视角：12 个固定站点最新状态（用于实时监控卡片）**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号（12 个固定站，顺序固定） |
| stnm | String | 站点名称（无匹配数据时为 `null`） |
| id | String | 站点 UUID |
| latestTm | String | 该站点最新观测时间（**实际记录时间**，ISO 格式 `yyyy-MM-ddTHH:mm:ss`，未经水文日调整）。无数据时返回**当前时间**，表示该时刻已检查但无测量值 |
| latestDrp | Number | 该站点最新 DRP 累计值 (mm)，无数据为 `null` |

**days[] — 日雨情视角：逐日雨量透视表（水文日维度，用于日雨情报表）**

| 字段 | 类型 | 说明 |
|------|------|------|
| day | String | 水文日标签 `yyyy-MM-dd 08:00:00` |
| values | Object | 键为 stcd，值为日降雨量 (mm)。无数据站点 = 0，**12 个 key 完整不缺** |
| avg | Number | 该水文日 12 站平均值 (mm)，保留 2 位小数 |

### 前端透视表用法

> `stations` 渲染实时卡片；`days` 渲染透视表。平均值已由后端计算返回。

```javascript
const resp = await fetch('/hltgq-site/st-pptn-r/reservoir-rainfall?startDate=2026-07-20&endDate=2026-07-25');
const { stations, days } = await resp.json();

// 表头：监测日期 + 12 站名称 + 平均值
const thead = `<tr><th>监测日期</th>${stations.map(s => `<th>${s.stnm}</th>`).join('')}<th>平均值 (mm)</th></tr>`;

// 表体：逐行渲染
const tbody = days.map(d => {
  const cells = stations.map(s => {
    const v = d.values[s.stcd] || 0;
    return `<td>${v > 0 ? v.toFixed(1) : ''}</td>`;
  });
  return `<tr><td>${d.day}</td>${cells.join('')}<td>${d.avg.toFixed(2)}</td></tr>`;
}).join('');
```

### 计算规则

完全对齐 `hydro-monitor.html` 的 `buildPptnPivot` 逻辑：

```
1. 向前后各扩展 1 天查询，确保水文日边界（8:00）完整
2. 按站点分组 → 时间升序 → 遍历计算增量
    第一条记录  →  inc = 0
    后续记录    →  inc = max(0, cur.DYP − prev.DYP)
3. 增量归属到水文日桶（getHydroDayLabel 规则）：
    标签 D 区间为 (D-1日 08:00:00, D日 08:00:00]（左开右闭）；
    8:00:00 整点归当日标签，08:00:01 起才归次日标签
4. 同一水文日内同一站点的所有增量累加 → 日降雨量
```

### 水文日切分示意

```
 (2026-07-28 08:00:00 ────── 2026-07-29 08:00:00]  →  标签 "2026-07-29 08:00:00"
 (2026-07-29 08:00:00 ────── 2026-07-30 08:00:00]  →  标签 "2026-07-30 08:00:00"
 8:00:00 整点属于当日标签（如 07-29 08:00:00 归标签 07-29），08:00:01 起归次日
```

---

## 七、水库时段雨情

12 个固定水库站点，按**可配时间间隔**聚合时段雨量，列含每站降雨量 + 平均值。

### 请求

```
GET /st-pptn-r/reservoir-period-rainfall
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| startDate | String | 否 | 当天 | 起始日期 `yyyy-MM-dd` |
| endDate | String | 否 | 当天 | 截止日期 `yyyy-MM-dd` |
| interval | int | 否 | 60 | 时间间隔（分钟），可选 15/30/45/60/120/180/240/360/480/720/1440 |

### 调用示例

```
/hltgq-site/st-pptn-r/reservoir-period-rainfall?startDate=2026-07-28&endDate=2026-07-29&interval=60
/hltgq-site/st-pptn-r/reservoir-period-rainfall?interval=180
```

### 响应

```json
{
  "stations": [
    { "stcd": "3206400001", "stnm": "周家河", ... }
  ],
  "buckets": [
    {
      "time": "2026-07-28 08:00",
      "values": { "3206400001": 1.2, "3206400002": 0.5, ... },
      "avg": 0.38
    },
    {
      "time": "2026-07-28 09:00",
      "values": { "3206400001": 2.3, "3206400002": 0.0, ... },
      "avg": 0.62
    }
  ]
}
```

### 字段说明

**stations[]** — 12 个站点信息，与 [六、水库实时雨情](#六水库实时雨情) 相同

**buckets[] — 时段桶数据：**

| 字段 | 类型 | 说明 |
|------|------|------|
| time | String | 时段标签 `yyyy-MM-dd HH:mm`（interval=60 时为整点） |
| values | Object | 键为 stcd，值为该时段降雨量 (mm)。12 个 key 完整不缺 |
| avg | Number | 该时段 12 站平均值 (mm)，保留 2 位小数 |

### 与日雨情的区别

| | 日雨情 `reservoir-rainfall` | 时段雨情 `reservoir-period-rainfall` |
|------|:--:|:--:|
| 时间维度 | **日**（水文日 08:00 切分） | **可配间隔**（分钟级） |
| 桶标签 | `yyyy-MM-dd 08:00:00` | `yyyy-MM-dd HH:mm` |
| 行平均值 | ✅ `avg` 字段 | ✅ `avg` 字段 |
| 参数 | startDate/endDate | startDate/endDate/**interval** |

### 间隔取值与桶覆盖范围

```
interval=60  → 从 (startDate-1) 08:00 开始，每小时一个桶，到 endDate 08:00 为止
              如 startDate=07-28, endDate=07-29 → 07-27 08:00 ~ 07-29 08:00 共 48 个桶

interval=180 → 同上，但每 3 小时一个桶，共 16 个桶
```

### 计算规则

完全对齐 `hydro-monitor.html` 的 `buildPeriodPivot` + `floorToInterval` 逻辑：

```
1. 向前后各扩展 1 天查询
2. 按站点分组 → 时间升序 → 遍历计算增量（与实时雨情相同）
    第一条  →  inc = 0
    后续    →  inc = max(0, cur.DYP − prev.DYP)
3. floorToInterval（水文日对齐）：先回退 1 秒（与 getHydroDayLabel 口径一致，8:00:00 整点归当日标签），
   再以当日 08:00 为桶起点按 interval 向下取整（如 08:35 → interval=60 时归属 08:00 桶）
   效果：8:00:00 整点增量归 07:00 时段桶不再丢失，时段合计与日雨情一致
4. 同一时段桶内同一站点的所有增量累加
```

---

## 八、水库旬月雨情

12 个固定水库站点，按月 × 旬（上/中/下旬）聚合雨量。列名：时间（年月）、旬段、12 个站点、平均值。

### 请求

```
GET /st-pptn-r/reservoir-ten-day-rainfall
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| year | Integer | 否 | 当年 | 年份，如 `2026` |
| startMonth | Integer | 否 | 1 | 起始月份（1~12），区间起点 |
| endMonth | Integer | 否 | 12 | 截止月份（1~12），区间终点；跨月区间自动归一化为起止顺序 |
| yearMonth | String | 否 | — | 旧参数兼容：年月 `yyyy-MM`，如 `2026-08`，等价于 year=2026&startMonth=8&endMonth=8 |

### 调用示例

```
/hltgq-site/st-pptn-r/reservoir-ten-day-rainfall?year=2026&startMonth=8&endMonth=8
/hltgq-site/st-pptn-r/reservoir-ten-day-rainfall?year=2026&startMonth=7&endMonth=8
/hltgq-site/st-pptn-r/reservoir-ten-day-rainfall?yearMonth=2026-08   （旧参数，单月）
```

### 响应

```json
{
  "stations": [
    { "stcd": "3206400001", "stnm": "周家河", ... }
  ],
  "periods": [
    {
      "yearMonth": "2026-08",
      "tenDay": "上旬",
      "values": { "3206400001": 15.2, "3206400002": 8.3, ... },
      "avg": 1.85
    },
    {
      "yearMonth": "2026-08",
      "tenDay": "中旬",
      "values": { "3206400001": 22.5, "3206400002": 12.1, ... },
      "avg": 2.56
    },
    {
      "yearMonth": "2026-08",
      "tenDay": "下旬",
      "values": { "3206400001": 18.0, "3206400002": 9.7, ... },
      "avg": 2.10
    }
  ]
}
```

### 字段说明

**stations[]** — 12 个站点信息，与 [六、水库实时雨情](#六水库实时雨情) 相同

**periods[] — 旬数据：**

| 字段 | 类型 | 说明 |
|------|------|------|
| yearMonth | String | 年月 `yyyy-MM` |
| tenDay | String | 旬段：`上旬` / `中旬` / `下旬` |
| values | Object | 键为 stcd，值为该旬累计降雨量 (mm)。12 个 key 完整不缺 |
| avg | Number | 该旬 12 站平均值 (mm)，保留 2 位小数 |

### 旬段划分

| 旬段 | 日期范围 |
|:--:|------|
| 上旬 | 1 日 ~ 10 日 |
| 中旬 | 11 日 ~ 20 日 |
| 下旬 | 21 日 ~ 月末 |

### 计算规则

```
1. 月份区间归一化：m1 = min(startMonth, endMonth)，m2 = max(startMonth, endMonth)
2. 查询 m1 月前一天 08:00 ~ m2 月最后一天次日 07:59:59 的完整数据（保证水文日边界）
3. 按站点分组 → 计算每日水文日雨量（同 reservoir-rainfall）
4. 逐月按旬汇总每日雨量 → 得到旬累计，跨月区间每个月份输出上/中/下旬 3 行
5. 12 站平均值 = Σ(各站旬累计) / 12
```

---

## 九、水库极值雨情

12 个固定水库站点，计算各时间窗口内的**最大降雨量**。列名：站点名称、最大 3 时、最大 6 时、最大 24 时、最大 2 天、最大 3 天、最大 7 天。

### 请求

```
GET /st-pptn-r/reservoir-extreme-rainfall
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| startDate | String | 否 | 当月 1 日 | 起始日期 `yyyy-MM-dd` |
| endDate | String | 否 | 当天 | 截止日期 `yyyy-MM-dd` |

### 调用示例

```
/hltgq-site/st-pptn-r/reservoir-extreme-rainfall
/hltgq-site/st-pptn-r/reservoir-extreme-rainfall?startDate=2026-07-01&endDate=2026-07-31
```

### 响应

```json
[
  {
    "stcd": "3206400001",
    "stnm": "周家河",
    "max3h": 12.5,
    "max6h": 18.3,
    "max24h": 45.2,
    "max2d": 52.1,
    "max3d": 58.7,
    "max7d": 89.3
  },
  {
    "stcd": "3206400002",
    "stnm": "姜家寨",
    "max3h": 8.2,
    "max6h": 15.1,
    "max24h": 38.6,
    "max2d": 42.0,
    "max3d": 48.5,
    "max7d": 75.2
  }
]
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| max3h | Number | 任意连续 3 小时最大雨量 (mm) |
| max6h | Number | 任意连续 6 小时最大雨量 (mm) |
| max24h | Number | 任意连续 24 小时最大雨量 (mm) |
| max2d | Number | 任意连续 2 天最大雨量 (mm) |
| max3d | Number | 任意连续 3 天最大雨量 (mm) |
| max7d | Number | 任意连续 7 天最大雨量 (mm) |

### 计算规则

```
1. 查询原始 DYP 记录（前扩 9 天保证 max7d 窗口及其基线完整，后扩 1 天覆盖 endDate 08:00 整点记录）
2. 按站点分组 → 计算 DYP 正向增量序列
3. 增量按小时桶（floorToInterval 水文日对齐）与水文日标签（getHydroDayLabel）双口径聚合
4. 滑动窗口求最大值：
    小时级（3h/6h/24h）→ 在小时桶序列上滑动求和
    天级（2d/3d/7d）→ 在水文日序列上滑动求和
5. 每个站点返回 6 个极值
```

---

## 十、水库雨情简报

指定日期各站点的**日 / 旬 / 月**三级雨量快报。列名：站点名称、日雨量、旬雨量、月雨量。

### 请求

```
GET /st-pptn-r/reservoir-rainfall-brief
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| date | String | 否 | 当天 | 指定日期 `yyyy-MM-dd` |

### 调用示例

```
/hltgq-site/st-pptn-r/reservoir-rainfall-brief
/hltgq-site/st-pptn-r/reservoir-rainfall-brief?date=2026-07-29
```

### 响应

```json
[
  {
    "stcd": "3206400001",
    "stnm": "周家河",
    "dailyRainfall": 5.2,
    "tenDayRainfall": 28.5,
    "monthlyRainfall": 85.3
  },
  {
    "stcd": "3206400002",
    "stnm": "姜家寨",
    "dailyRainfall": 3.1,
    "tenDayRainfall": 18.2,
    "monthlyRainfall": 62.7
  }
]
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| dailyRainfall | Number | 指定日的水文日雨量 (mm) |
| tenDayRainfall | Number | 指定日所在旬的累计雨量 (mm) |
| monthlyRainfall | Number | 指定日所在月的累计雨量 (mm) |

### 旬归属

| 日期 | 归属旬 |
|:--:|:--:|
| 1~10 日 | 上旬 |
| 11~20 日 | 中旬 |
| 21~月末 | 下旬 |

### 计算规则

```
1. 输入日期 → 确定所在月份 + 旬段
2. 查询该月完整 DRP 数据 → 按日水文日计算增量
3. 日雨量 = 目标水文日的增量总和
4. 旬雨量 = 该旬所有水文日的增量总和
5. 月雨量 = 该月所有水文日的增量总和
```

---

## 十一、站点综合指标

一次性返回所有站点的**水位 + 雨量**综合概览，用于监控首页卡片。

### 请求

```
GET /station-metrics
```

无参数，返回全量站点。

### 响应

```json
[
  {
    "stcd": "3206400001",
    "stnm": "周家河",
    "z": 12.35,
    "riverTm": "2026-07-29T10:30:00",
    "drp": 5.2,
    "pptnTm": "2026-07-28",
    "type": "all"
  },
  {
    "stcd": "9000000137",
    "stnm": "某某雨量站",
    "z": null,
    "riverTm": null,
    "drp": 3.1,
    "pptnTm": "2026-07-28",
    "type": "rain"
  }
]
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| z | Number | 最新水位值 (m)，无数据为 `null` |
| riverTm | String | 水位最新观测时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`），无数据为 `null` |
| drp | Number | 当日水文日累计降雨 (mm)，无雨量站点为 `null` |
| pptnTm | String | 雨量所属水文日日期，无雨量站点为 `null` |
| type | String | 站点类型：`all`=水位+雨量 / `water`=仅水位 / `rain`=仅雨量 |

### type 判定逻辑

```
ST_RIVER_R 表有该站点的水位记录 → 是水位站
rain_info 表有该站点的雨量记录 → 是雨量站

两者都有 → all
仅水位   → water
仅雨量   → rain
```

---

## 十二、灌区水位历史数据

单站点小时级水位历史记录，分页返回，每条含监测时间 + 水位值 (m) + 1h 涨幅 (cm)。

### 请求

```
GET /irrigation-water-level/history
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| stcd | String | **是** | — | 站点编号 |
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 20 | 每页条数 |
| startTime | String | 否 | 7 天前整点 | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | 当前整点小时 | 截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/irrigation-water-level/history?stcd=9000000137&page=1&size=20
/hltgq-site/irrigation-water-level/history?stcd=9000000137&startTime=2026-07-20+08:00:00&endTime=2026-07-25+08:00:00
```

### 响应

```json
{
  "total": 168,
  "current": 1,
  "size": 20,
  "pages": 9,
  "records": [
    { "hour": "2026-07-22 17:00", "waterLevel": 12.35, "change": null },
    { "hour": "2026-07-22 18:00", "waterLevel": 12.40, "change": 5.0 },
    { "hour": "2026-07-22 19:00", "waterLevel": 12.38, "change": -2.0 }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总小时数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按时间升序**） |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| hour | String | 监测时间 `yyyy-MM-dd HH:00` |
| waterLevel | Number | 水位值 (m)。无数据时为 `null` |
| change | Number | 1h 水位涨幅 (cm)。正值=上涨，负值=下降。首小时为 `null` |

### 计算规则

与 `/irrigation-water-level/chart` 完全一致：

```
1. 向前扩展 1h 查询原始水位记录（确保首小时有基线对比）
2. 按整点小时聚合：同一小时内取最新一条水位值
3. 水位变化 (cm) = (当前小时水位 − 前一小时水位) × 100
4. 生成完整小时序列（严格 1h 步长，无缺失），再内存分页
5. 当前小时无数据时，水位值为 null，前值延续
```

### 与 /chart 的区别

| | `/chart` | `/history` |
|------|:--:|:--:|
| 用途 | ECharts 曲线图（双 Y 轴） | 数据表格（分页浏览） |
| 分页 | ❌ 全部返回 | ✅ 按 page/size 分页 |
| 包裹信息 | 含 stcd / stnm / startTime / endTime | 仅分页元信息 + records |
| 排序 | 时间升序 | 时间升序 |

---

## 十三、站点分类查询

返回四种监测类型的站点编号/名称清单，支持按类型筛选或全量分组返回。用于前端选择器（下拉框 / 站点列表）。

### 请求

```
GET /station-metrics/sites
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| type | String | 否 | — | 可选筛选：`rainfall` / `gq-rainfall` / `waterLevel` / `gate` / `flow` / `moisture`。不传则返回全量分组 |

### 调用示例

```
/hltgq-site/station-metrics/sites
/hltgq-site/station-metrics/sites?type=rainfall
/hltgq-site/station-metrics/sites?type=gq-rainfall
/hltgq-site/station-metrics/sites?type=waterLevel
/hltgq-site/station-metrics/sites?type=gate
/hltgq-site/station-metrics/sites?type=flow
/hltgq-site/station-metrics/sites?type=moisture
```

### 响应（不传 type — 全量分组）

```json
{
  "rainfall": [
    { "code": "9000000136", "name": "渠道管理处" },
    { "code": "9000000137", "name": "桐城(车富岭)" }
  ],
  "waterLevel": [
    { "code": "9000000136", "name": "渠道管理处" }
  ],
  "gate": [
    { "code": "dByBR1Xdze8shnRGAw9", "name": "南岳寺泄洪闸" }
  ],
  "flow": [
    { "code": "9000000004", "name": "某某流量站" }
  ],
  "moisture": [
    { "code": "9000000101", "name": "某某墒情站" }
  ]
}
```

### 响应（传 type=rainfall）

```json
[
  { "code": "9000000136", "name": "渠道管理处" },
  { "code": "9000000137", "name": "桐城(车富岭)" }
]
```

### 字段说明

**不传 type（分组）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| rainfall | Array\<Site\> | 雨量监测站点 |
| waterLevel | Array\<Site\> | 水位监测站点 |
| gate | Array\<Site\> | 闸门监测站点 |
| flow | Array\<Site\> | 流量监测站点 |
| moisture | Array\<Site\> | 墒情监测站点 |

**传 type（扁平列表）：**

返回 `Array<Site>`，其中 **Site 对象**：

| 字段 | 类型 | 说明 |
|------|------|------|
| code | String | 站点编号 |
| name | String | 站点名称 |

### 分类来源

| type 值 | 数据源 | 说明 |
|------|------|------|
| rainfall | `t_auto_hltgq_water_rain_info` | DISTINCT STCD + st_stinfo 查名 |
| gq-rainfall | `t_auto_hltgq_water_rain_info` | 灌区雨量：排除水库 13 站（STCD + 名称双重排除）后的雨量站 |
| waterLevel | `t_auto_hltgq_water_river_info` | DISTINCT STCD + 关联表查名 |
| gate | `t_auto_hltgq_water_gate` | DISTINCT site + siteName |
| flow | `t_auto_hltgq_water_wt_nfo` | DISTINCT stcd + 站点信息表查名 |
| moisture | `t_auto_hltgq_water_nmisp_info` | DISTINCT 站点标识（COALESCE(stcd, site)）+ 站点信息表查名 |

---

## 十四、水库水位-河道水情

仅限两个河道水位站（周家河、花凉亭坝下），返回监测时间、警戒水位、保证水位、水位值、水势。支持日期区间多站合并分页查询（时间倒序）。

> 页面按水文日口径传参：选中日期区间 [起, 止] 时传 `startTime=起 08:00:00`、`endTime=止+1天 07:59:59`，与雨情/日时段/简报的 8 点日界一致。

### 请求

```
GET /st-river-r/regime
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| stcd | String | **是** | — | 站点编号，**支持逗号分隔多站**：`3206400001`（周家河）、`320640000A`（花凉亭坝下） |
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 20 | 每页条数 |
| startTime | String | 否 | — | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | — | 截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/st-river-r/regime?stcd=3206400001&page=1&size=20
/hltgq-site/st-river-r/regime?stcd=3206400001,320640000A&startTime=2026-07-20+08:00:00&endTime=2026-07-31+07:59:59
```

### 响应

```json
{
  "total": 100,
  "current": 1,
  "size": 20,
  "pages": 5,
  "records": [
    {
      "stcd": "3206400001",
      "stnm": "周家河",
      "tm": "2026-07-29T08:00:00",
      "warningLevel": null,
      "guaranteedLevel": null,
      "z": 12.35,
      "wptn": "平"
    },
    {
      "stcd": "3206400001",
      "stnm": "周家河",
      "tm": "2026-07-29T08:30:00",
      "warningLevel": null,
      "guaranteedLevel": null,
      "z": 12.40,
      "wptn": "涨"
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按时间倒序，最新在前**） |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| tm | String | 监测时间（ISO 格式） |
| warningLevel | Number | 警戒水位 (m)。站点固定参考值，未配置时为 `null` |
| guaranteedLevel | Number | 保证水位 (m)。站点固定参考值，未配置时为 `null` |
| z | Number | 水位值 (m) |
| wptn | String | 水势：`涨` / `落` / `平` / `无涨落信息` |

### 导出接口（全量，无分页）

```
GET /st-river-r/regime/export
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| stcd | String | **是** | 站点编号，支持逗号分隔多站 |
| startTime | String | 否 | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | 截止时间 `yyyy-MM-dd HH:mm:ss` |

> 返回 `List<RiverRegimeVO>`（字段同 records[]，按时间倒序）。用于前端 Excel 导出，避免只导出当前分页。

### WPTN 水势映射

| 数据库编码 | 返回文本 |
|:--:|------|
| `"4"` / `"涨"` | 涨 |
| `"5"` / `"落"` | 落 |
| `"6"` / `"平"` | 平 |
| null / 空串 / 其他 | 无涨落信息 |

### 限定站点

| stcd | 站点名称 |
|------|------|
| `3206400001` | 周家河 |
| `320640000A` | 花凉亭坝下 |

> 传入其他 stcd 将返回 500 错误，提示仅支持这两个站点。

---

## 十五、水库水位-水库水情

水库水位站点（花凉亭坝上），返回监测时间、警戒水位、保证水位、水位值、水势、入库流量、出库流量。支持日期区间多站合并分页查询（时间倒序）。

> 页面按水文日口径传参：选中日期区间 [起, 止] 时传 `startTime=起 08:00:00`、`endTime=止+1天 07:59:59`。

> **注意**：入库流量 (`inq`) 和出库流量 (`otq`) 目前均映射到数据库 `Q` 字段（保留 3 位小数），待设备报文到位后区分字段映射。

### 请求

```
GET /st-river-r/reservoir-regime
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| stcd | String | **是** | — | 站点编号，**支持逗号分隔多站**：目前仅 `3206400007`（花凉亭坝上） |
| page | int | 否 | 1 | 页码 |
| size | int | 否 | 20 | 每页条数 |
| startTime | String | 否 | — | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | — | 截止时间 `yyyy-MM-dd HH:mm:ss` |

### 调用示例

```
/hltgq-site/st-river-r/reservoir-regime?stcd=3206400007&page=1&size=20
/hltgq-site/st-river-r/reservoir-regime?stcd=3206400007&startTime=2026-07-20+08:00:00&endTime=2026-07-31+07:59:59
```

### 响应

```json
{
  "total": 100,
  "current": 1,
  "size": 20,
  "pages": 5,
  "records": [
    {
      "stcd": "3206400007",
      "stnm": "花凉亭坝上",
      "tm": "2026-07-29T08:00:00",
      "warningLevel": null,
      "guaranteedLevel": null,
      "z": 82.35,
      "wptn": "平",
      "inq": 15.2,
      "otq": 15.2
    },
    {
      "stcd": "3206400007",
      "stnm": "花凉亭坝上",
      "tm": "2026-07-29T08:30:00",
      "warningLevel": null,
      "guaranteedLevel": null,
      "z": 82.40,
      "wptn": "涨",
      "inq": 16.8,
      "otq": 16.8
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按时间倒序，最新在前**） |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| tm | String | 监测时间（ISO 格式） |
| warningLevel | Number | 警戒水位 (m)。站点固定参考值，未配置时为 `null` |
| guaranteedLevel | Number | 保证水位 (m)。站点固定参考值，未配置时为 `null` |
| z | Number | 水位值 (m) |
| wptn | String | 水势：`涨` / `落` / `平` / `无涨落信息` |
| inq | Number | 入库流量 (m³/s)。暂映射 Q 字段，保留 3 位小数 |
| otq | Number | 出库流量 (m³/s)。暂映射 Q 字段，保留 3 位小数 |

### 导出接口（全量，无分页）

```
GET /st-river-r/reservoir-regime/export
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| stcd | String | **是** | 站点编号，支持逗号分隔多站 |
| startTime | String | 否 | 起始时间 `yyyy-MM-dd HH:mm:ss` |
| endTime | String | 否 | 截止时间 `yyyy-MM-dd HH:mm:ss` |

> 返回 `List<ReservoirRegimeVO>`（字段同 records[]，按时间倒序）。用于前端 Excel 导出，避免只导出当前分页。

### 限定站点

| stcd | 站点名称 |
|------|------|
| `3206400007` | 花凉亭坝上 |

> 传入其他 stcd 将返回 500 错误，提示仅支持花凉亭坝上。

### 与河道水情（/regime）的区别

| | `/regime` | `/reservoir-regime` |
|------|:--:|:--:|
| 站点类型 | 河道水位 | **水库水位** |
| 站点范围 | 周家河、花凉亭坝下 | 花凉亭坝上 |
| 额外字段 | — | `inq`（入库流量）、`otq`（出库流量） |
| 数据表 | `t_auto_hltgq_water_river_info` | `t_auto_hltgq_water_river_info` |

---

## 十六、水库水情-水情简报

按查询日期返回三个水情站（周家河、花凉亭坝下、花凉亭坝上）的 8 点/20 点水位、水势、流量、当年最高水位等简报数据，每站一行。

### 请求

```
GET /st-river-r/water-brief
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| date | String | 否 | 当天 | 查询日期 `yyyy-MM-dd` |

### 调用示例

```
/hltgq-site/st-river-r/water-brief
/hltgq-site/st-river-r/water-brief?date=2026-08-14
```

### 响应

```json
[
  {
    "stcd": "3206400001",
    "stnm": "周家河",
    "wrz": 88.50,
    "y8": 87.93,
    "y20": 87.93,
    "t8": 88.93,
    "wptn": "落",
    "cmp": 1.00,
    "q": 162.50,
    "w": null,
    "maxz": 89.10,
    "maxTm": "2026-06-03T08:00:00",
    "dsflz": 88.93
  }
]
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| wrz | Number | 警戒水位 (m)，阈值表 threshold，未配置为 `null` |
| y8 | Number | 昨日 8 点水位 (m)：昨日 (07:00, 08:00] 内 z 非空的最新一条（8 点前最后一条采集） |
| y20 | Number | 昨日 20 点水位 (m)：昨日 (19:00, 20:00] 内 z 非空的最新一条（20 点前最后一条采集） |
| t8 | Number | 今天 8 点水位 (m)：查询日 (07:00, 08:00] 内 z 非空的最新一条（8 点前最后一条采集） |
| wptn | String | 水势：`涨` / `落` / `平` / `无涨落信息`（优先取 t8 记录，无则取当日最新记录） |
| cmp | Number | 与昨日 8 点比 (m) = t8 − y8，可正可负，任一侧缺数时为 `null` |
| q | Number | 流量 (m³/s)，取 t8 记录 Q 字段，无记录为 `null` |
| w | Number | 蓄水量（百万 m³）。暂无数据源，恒为 `null`，前端显示 `-` |
| maxz | Number | 当年最高水位 (m)：当年 1 月 1 日以来（截至查询日）最大值 |
| maxTm | String | 当年最高水位出现时间（ISO 格式，同高取最早），当年无数据为 `null` |
| dsflz | Number | 设防水位 (m)，阈值表 guarantee，未配置为 `null` |

### 计算规则

- 8 点/20 点水位取「整点之前的最后一条采集」（业主口径）：即在 (整点-1h, 整点] 左开右闭区间内 z 非空的最新一条记录，整点之后的采集归下一整点。一旦进入下一时段，该整点数值固定不再变动（日时段水情表的槽位取值遵循同一口径）
- 覆盖站点固定为：周家河 `3206400001`、花凉亭坝下 `320640000A`、花凉亭坝上 `3206400007`

---

## 十七、水库水情-多年同期水情

按年份区间 + 月份查询各站该月平均水位，每个年份一行，值与 stations 顺序对齐。

### 请求

```
GET /st-river-r/years-regime
```

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:--:|--------|------|
| startYear | int | **是** | — | 起始年份（含），如 2011 |
| endYear | int | **是** | — | 结束年份（含），如 2012 |
| month | int | **是** | — | 月份（1-12），如 7 |

### 调用示例

```
/hltgq-site/st-river-r/years-regime?startYear=2011&endYear=2012&month=7
```

### 响应

```json
{
  "stations": ["周家河", "花凉亭坝下", "花凉亭坝上"],
  "rows": [
    { "tm": "2011-07", "values": [88.35, 76.20, 80.10] },
    { "tm": "2012-07", "values": [89.10, 77.55, null] }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stations | Array\<String\> | 站点名称列表（表头列），固定三个水情站 |
| rows | Array | 数据行，每个年份一行 |

**rows[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| tm | String | 监测日期 `yyyy-MM` |
| values | Array\<Number\> | 各站该月平均水位 (m)，与 stations 一一对应；无数据为 `null` |

### 计算规则

- 按自然月（1 日 00:00 ~ 月末 23:59:59）对水位 Z 求平均，截断 2 位小数
- 覆盖站点固定为：周家河、花凉亭坝下、花凉亭坝上

---

## 十八、闸门监测

> 每个闸站返回一条记录，包含该站所有闸孔的最新开度、闸前/闸后水位及状态，支持按日期区间筛选。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/monitoring`

### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| site | 否 | String | 站点 UUID，不传=全部站点 |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间（含），不传则不限制 |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间（含），不传则不限制 |

### 响应格式

```json
[
  {
    "siteId": "uuid",
    "siteName": "南山寺节制闸",
    "tm": "2024-06-15T10:30:00",
    "upZ": 33.20,
    "downZ": 31.50,
    "q": 2.458,
    "vol": 12.5,
    "isOnline": true,
    "lon": 116.2345,
    "lat": 30.5123,
    "holes": [
      {
        "gateNo": "1",
        "openDegree": 1.20,
        "status": "停止"
      },
      {
        "gateNo": "2",
        "openDegree": 0.00,
        "status": "停止"
      }
    ]
  }
]
```

### 字段说明

**外层字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| siteId | String | 站点 UUID |
| siteName | String | 站点名称 |
| tm | String | 监测时间（ISO 格式），取该站各闸孔中最新的时间 |
| upZ | Number | 闸前水位 (m) — 上游水位。取各闸孔中最新有值记录 |
| downZ | Number | 闸后水位 (m) — 下游水位。取各闸孔中最新有值记录 |
| q | Number | 流量 (m³/s)，取关联流量表 `t_auto_hltgq_water_wt_nfo` 最新值，无数据为 `null` |
| vol | Number | 电压 (V)，取关联电压表 `t_auto_hltgq_water_vol_info` 最新值，无数据为 `null` |
| isOnline | Boolean | 在线状态：`true` 在线 / `false` 断联。按最新监测时间断联判定：MQTT 站（南山寺节制闸、渠首进水闸、渠首电站防洪闸、双庙湖节制闸）30 分钟无更新判离线，其余 RabbitMQ 站 70 分钟无更新判离线；无监测时间默认在线 |
| lon | Number | 站点经度（站点表 bviiio_x） |
| lat | Number | 站点纬度（站点表 bviiio_y） |
| holes | Array | 各闸孔数据（按闸孔号升序排列） |

**holes[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| gateNo | String | 闸孔编号，如 `"1"`、`"2"`、`"3"` |
| openDegree | Number | 闸门开度 (m)。前端展示为"开度1"、"开度2"… |
| status | String | 闸门状态，如 `开启` / `关闭` / `停止` |

### 数据来源

| 数据表 | 说明 |
|------|------|
| `t_auto_hltgq_water_gate` | 闸门实时监测数据 |
| `t_auto_hltgq_5nw74_vnqqef` | 站点信息表（关联取站点名称） |

### 查询逻辑

1. 使用 PostgreSQL `DISTINCT ON (site, gate_no)` 对每个站点的每个闸孔取 `tm` 最新的一条记录
2. 支持可选的 `startTime` / `endTime` 参数限定时间范围（仅在该范围内的数据参与查询）
3. 服务层按 `site` 分组，将同一站点的多个闸孔聚合成一条 `GateMonitoringVO` 记录
4. 闸前/闸后水位取各闸孔中最新有值记录的 `up_z` / `down_z`（同一时刻各孔水位原则上一致）

---

### 闸门设备列表

> 根据站点 siteId 返回该站各闸孔的设备名称、记录 ID 和闸孔编号。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/devices`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | **是** | String | 站点 UUID |

#### 响应格式

```json
[
  {
    "id": "uuid-xxx",
    "gateNo": "1",
    "deviceName": "南山寺节制闸1#"
  },
  {
    "id": "uuid-yyy",
    "gateNo": "2",
    "deviceName": "南山寺节制闸2#"
  }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 闸孔记录 ID |
| gateNo | String | 闸孔编号（如 "1"、"2"） |
| deviceName | String | 设备名称（如"南山寺节制闸2#"），由 stnm + gateNo + "#" 拼接 |

#### 查询逻辑

1. 使用 PostgreSQL `DISTINCT ON (gate_no)` 对每个闸孔取 `tm` 最新的一条记录
2. `deviceName` 由关联站点表的 `zzkaec`（站点简称） + `gate_no` + `#` 拼接而成

---

### 闸门历史数据

> 按监测时间分页返回闸门历史数据，**type 参数决定返回结构**（开度透视 / 水位 / 流量三种形态）。支持按站点、日期区间筛选。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/history`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | 否 | String | 站点 UUID，不传=全部站点 |
| type | **是** | String | 数据类型：`opening`（开度）/ `waterLevel`（水位）/ `flow`（流量） |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间（含），不传则不限制 |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间（含），不传则不限制 |
| page | 否 | long | 页码，默认 1 |
| size | 否 | long | 每页条数，默认 10 |

#### 响应格式（type=opening — 开度透视）

```json
{
  "total": 500,
  "current": 1,
  "size": 10,
  "pages": 50,
  "records": [
    { "tm": "2026-08-16 17:30:00", "open1": 1.20, "open2": 0.00, "open3": 1.05, "q": 2.458 },
    { "tm": "2026-08-16 17:25:00", "open1": 1.18, "open2": 0.00, "open3": 1.02, "q": 2.452 }
  ]
}
```

#### 响应格式（type=waterLevel — 水位）

```json
{
  "total": 500,
  "current": 1,
  "size": 10,
  "pages": 50,
  "records": [
    { "tm": "2026-08-16 17:30:00", "upZ": 33.20, "downZ": 31.50, "q": 2.458 },
    { "tm": "2026-08-16 17:25:00", "upZ": 33.18, "downZ": 31.48, "q": 2.452 }
  ]
}
```

#### 响应格式（type=flow — 流量）

```json
{
  "total": 150,
  "current": 1,
  "size": 10,
  "pages": 15,
  "records": [
    { "tm": "2026-08-16 17:30:00", "q": 2.458, "tf": 12500.6 },
    { "tm": "2026-08-16 17:25:00", "q": 2.452, "tf": 12498.3 }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按监测时间倒序**，最新在前） |

**records[].字段（按 type 不同）：**

| type | 行字段 | 说明 |
|------|------|------|
| opening | tm / open1, open2... / q | 按监测时间分组的开度透视：每个闸孔号一列（`open` + 闸孔号）；`q` 为站点流量（同一时刻各孔相同，仅放入一次） |
| waterLevel | tm / upZ / downZ / q | 闸前水位 (m)、闸后水位 (m)、站点流量 (m³/s)。同一站点各闸孔水位相同，取首个非空值 |
| flow | tm / q / tf | 瞬时流量 (m³/s)、累计流量 (m³)。数据来自流量表 `t_auto_hltgq_water_wt_nfo`（site = 闸站 UUID） |

> **注意**：`opening` 与 `waterLevel` 数据来自闸门表 `t_auto_hltgq_water_gate`，按监测时间去重分页；`flow` 来自流量表 `t_auto_hltgq_water_wt_nfo`，分页口径独立。

---

### 闸站图表-固定七站水位

> 固定七站按选中时间点（半小时粒度）返回闸前/闸后水位。取各站距选中时间点最近（±30 分钟内）的闸门入库数据；半小时内无入库数据则该站水位为 `null`（该时间点无报文）。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/station-water-level`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| time | **是** | `yyyy-MM-dd HH:mm` | 选中时间点（半小时粒度，如 `2026-08-16 17:30`，一天 48 个可选时间点） |

#### 调用示例

```
/hltgq-site/gate-monitor/station-water-level?time=2026-08-16+17:30
```

#### 响应格式

```json
[
  { "id": "uuid-qsjsz", "stcd": "QSJSZ",       "name": "渠首进水闸",   "upZ": 32.15, "downZ": 31.28 },
  { "id": "uuid-smh",   "stcd": "SMH",         "name": "双庙湖节制闸", "upZ": 33.20, "downZ": 31.50 },
  { "id": "uuid-nss",   "stcd": "NSS",         "name": "南山寺节制闸", "upZ": 30.85, "downZ": null },
  { "id": "uuid-9005",  "stcd": "9000000005",  "name": "毕岭节制闸",   "upZ": null,  "downZ": null },
  { "id": "uuid-9006",  "stcd": "9000000006",  "name": "汪元节制闸",   "upZ": 29.72, "downZ": 28.90 },
  { "id": "uuid-9001",  "stcd": "9000000001",  "name": "北干渠进水闸", "upZ": 34.10, "downZ": null },
  { "id": "uuid-9002",  "stcd": "9000000002",  "name": "南干渠进水闸", "upZ": 33.95, "downZ": null }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 站点 ID（站点表 UUID，对应闸门表 site） |
| stcd | String | 测站编码（部分站点无 stcd 时为空，正常现象） |
| name | String | 闸站名称（取站点表实际值，北/南干渠进水闸不带"（上游水位）"后缀） |
| upZ | Number | 闸前水位 (m) — 上游水位。半小时内无入库数据为 `null`（前端展示 `-`）；`-9991` 表示设备异常（前端展示 `--`）；`-999` 表示设备不存在（前端不展示） |
| downZ | Number | 闸后水位 (m) — 下游水位。北/南干渠进水闸只有上游水位，下游水位自然为 `null` |

#### 固定七站（顺序固定）

| 顺序 | stcd | 站点名称 |
|:--:|------|------|
| 1 | `QSJSZ` | 渠首进水闸 |
| 2 | `SMH` | 双庙湖节制闸 |
| 3 | `NSS` | 南山寺节制闸 |
| 4 | `9000000005` | 毕岭节制闸 |
| 5 | `9000000006` | 汪元节制闸 |
| 6 | `9000000001` | 北干渠进水闸（上游水位） |
| 7 | `9000000002` | 南干渠进水闸（上游水位） |

#### 查询逻辑

1. 按固定七站编码查站点表，得到站点 UUID 列表（站点表主键 `iofhpi` = 测站编码）
2. 闸门表在 `[time−30min, time+30min]` 窗口内查询，`DISTINCT ON (site)` + 按与 `time` 的时间距离升序，取每站距选中时间点最近的一条入库水位
3. 按固定顺序组装 7 个站点；半小时内无入库数据的站点水位为 `null`（表示该时间点无报文）
4. `-9991`（设备异常）透传由前端展示 `--`，`-999`（设备不存在）透传由前端不展示，与闸门其他接口口径一致

---

### 闸门站点列表

> 返回有闸门数据的站点列表（关联站点表取名称，按站名排序），供页面下拉选择站点。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/sites`

无参数。

#### 响应格式

```json
[
  { "site": "uuid-xxx", "siteName": "南山寺节制闸" },
  { "site": "uuid-yyy", "siteName": "双庙湖节制闸" }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| site | String | 站点 UUID（对应闸门表 site） |
| siteName | String | 站点名称（站点表 zzkaec） |

#### 查询逻辑

1. 闸门表 `DISTINCT site` 取有闸门数据的站点集合
2. 关联站点表 `t_auto_hltgq_5nw74_vnqqef`（`site = id`）取 `zzkaec` 作为站点名称
3. 按站点名称升序排列

---

### 闸孔编号列表

> 返回指定站点下的闸孔编号列表（升序），供页面下拉选择闸孔。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/gates`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | **是** | String | 站点 UUID |

#### 响应格式

```json
["1", "2", "3"]
```

#### 说明

- 闸孔编号升序排列
- 库中历史采集占位行 `gate_no = '0'` 统一归一化为 `'1'`（与 monitoring/history 口径一致）

---

### 闸门数据导出（小时级聚合）

> 按站点 + 可选多闸孔查询，按小时聚合（AVG），供页面导出 Excel 使用。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/export`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | **是** | String | 站点 UUID |
| gateNos | 否 | String | 闸孔编号列表，逗号分隔（如 `1,2,3`），不传=全部闸孔 |
| startTime | **是** | `yyyy-MM-dd HH:mm:ss` | 开始时间 |
| endTime | **是** | `yyyy-MM-dd HH:mm:ss` | 结束时间 |

#### 响应格式

```json
[
  {
    "tm": "2026-08-16T17:00:00",
    "site": "uuid-xxx",
    "gateNo": "1",
    "openDegree": 1.20,
    "upZ": 33.20,
    "downZ": 31.50,
    "q": 2.458,
    "tf": 12500.60
  }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| tm | String | 小时桶起始时间（ISO 格式，`date_trunc('hour', tm)`） |
| site | String | 站点 UUID |
| gateNo | String | 闸孔编号（`'0'` 归一化为 `'1'`） |
| openDegree | Number | 该小时平均开度 (m)，`-999`/`-9991` 已过滤 |
| upZ | Number | 该小时平均闸前水位 (m)，`-999`/`-9991` 已过滤 |
| downZ | Number | 该小时平均闸后水位 (m)，`-999`/`-9991` 已过滤 |
| q | Number | 该小时平均流量 (m³/s)，来自流量表 `t_auto_hltgq_water_wt_nfo` |
| tf | Number | 该小时累计流量 (m³) 最大值 |

#### 聚合规则

1. 闸门表按小时桶（`date_trunc('hour', tm)`）+ 闸孔分组，开度/水位取 `AVG`（`-9991` 异常值、`-999` 不存在值已过滤）
2. 流量表按站点 + 小时桶预聚合后 `LEFT JOIN`，同一小时无流量数据时 q/tf 为 `null`
3. 结果按小时升序排列

---

### 闸站累计流量

> 返回指定闸站月累计取水量 + 年累计取水量。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/cumulative-flow`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | **是** | String | 站点 UUID，如渠首进水闸 `CAYQ739MiBWMg9gQvyi` |
| monthStart | 否 | `yyyy-MM-dd HH:mm:ss` | 月累计起点，默认当月 1 日 0 点 |

#### 响应格式

```json
{
  "siteId": "CAYQ739MiBWMg9gQvyi",
  "siteName": "渠首进水闸",
  "monthCumulativeFlow": 12500.60,
  "yearCumulativeFlow": 152300.45
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| siteId | String | 站点 UUID |
| siteName | String | 站点名称（站点表未查到为 `null`） |
| monthCumulativeFlow | Number | 月累计流量 (m³)，2 位小数 = ttf(最新) − ttf(monthStart 前最近行)；月内无 ttf 数据为 `null` |
| yearCumulativeFlow | Number | 年累计流量 (m³)，2 位小数 = 当年 1 月 1 日 0 点起至最新数据时间的 `ytf`；无数据为 `null` |

#### 计算口径

1. 月累计：`tm ≥ monthStart`（默认当月 1 日 0 点）内取最新 `ttf`，减 `monthStart` 前最近一条 `ttf`；起点前无积分行基准按 0
2. 年累计：当年 1 月 1 日 0 点起，取最新 `ytf`（无记录为 `null`）
3. 数据来源：流量表 `t_auto_hltgq_water_wt_nfo`；`-999`（设备不存在）→ `null`，`-9991`（设备异常）保留透传由前端展示

---

### 闸站月度累计取水量趋势

> 近 N 个月（含当月）的月度累计取水量趋势，供月度趋势图表；每月一个数据点，时间升序（最早在前）。

**请求方式**：`GET`

**请求路径**：`/gate-monitor/monthly-cumulative-flow`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | **是** | String | 站点 UUID，如渠首进水闸 `CAYQ739MiBWMg9gQvyi` |
| months | 否 | int | 月数，默认 12，上限 24（超上限截断，≤0 报参数错误）；当前月为最后一个月 |

#### 响应格式

```json
[
  { "month": "2025-09", "cumulativeFlow": 10850.25 },
  { "month": "2025-10", "cumulativeFlow": null },
  { "month": "2025-11", "cumulativeFlow": 9630.80 }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| month | String | 月份 `yyyy-MM`，时间升序（最早在前） |
| cumulativeFlow | Number | 月累计取水量 (m³)，2 位小数；月内无 ttf 数据为 `null` |

#### 计算口径

1. 月累计口径同 [闸站累计流量](#闸站累计流量)：当月 1 日 0 点 ≤ tm < 下月 1 日 0 点 = ttf(月内最新) − ttf(月初前最近)
2. 当月累计截至最新数据时间；月初前无积分行（最早月无历史数据）基准按 0
3. 返回月份连续（每月一个数据点），月内无 ttf 数据时累计为 `null`

---

## 十九、流量监测

### 19.1 流量监测-最新数据

> 每个站点返回一条最新记录，包含站点名称、监测时间、流量。支持根据站点编号（多选）和日期区间筛选。

**请求方式**：`GET`

**请求路径**：`/flow-monitor/monitoring`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcds | 否 | String | 站点编号，多个逗号分隔（如 `9000000004,9000000005`）。不传=全部 |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间（含），不传则不限制 |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间（含），不传则不限制 |

#### 响应格式

```json
[
  {
    "stcd": "9000000004",
    "stnm": "某某流量站",
    "tm": "2026-07-29T18:00:00",
    "q": 2.458
  }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| tm | String | 监测时间（ISO 格式） |
| q | Number | 流量 (m³/s) |

---

### 19.2 流量趋势图表

> 小时级流量曲线，默认近七天数据，支持根据日期区间查询。

**请求方式**：`GET`

**请求路径**：`/flow-monitor/trend`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcd | **是** | String | 站点编号 |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间，默认 7 天前整点 |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间，默认当前整点 |

#### 响应格式

```json
{
  "stcd": "9000000004",
  "stnm": "某某流量站",
  "startTime": "2026-07-22T19:00:00",
  "endTime": "2026-07-29T19:00:00",
  "hours": [
    { "hour": "2026-07-22 19:00", "flow": 2.558 },
    { "hour": "2026-07-22 20:00", "flow": null },
    { "hour": "2026-07-22 21:00", "flow": 2.569 }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| startTime | String | 查询起始时间（ISO 格式） |
| endTime | String | 查询截止时间（ISO 格式） |
| hours | Array | 小时级数据点（**严格 1h 步长，无缺失**） |

**hours[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| hour | String | 小时标签 `yyyy-MM-dd HH:00` |
| flow | Number | 该小时平均流量 (m³/s)。无数据时为 `null` |

#### 计算规则

1. 查询原始流量记录（按时间升序）
2. 按整点小时聚合：同一小时内多条记录取平均值
3. 生成完整小时序列（从 startTime 到 endTime，步长固定 1h）
4. 无数据小时 `flow` 为 `null`

---

### 19.3 流量历史数据

> 分页返回指定站点的历史流量记录（按监测时间倒序），支持根据日期区间查询。

**请求方式**：`GET`

**请求路径**：`/flow-monitor/history`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcd | **是** | String | 站点编号 |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间（含） |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间（含） |
| page | 否 | long | 页码，默认 1 |
| size | 否 | long | 每页条数，默认 20 |

#### 响应格式

```json
{
  "total": 150,
  "current": 1,
  "size": 20,
  "pages": 8,
  "records": [
    {
      "stcd": "9000000004",
      "stnm": "某某流量站",
      "tm": "2026-07-29T18:00:00",
      "q": 2.458
    },
    {
      "stcd": "9000000004",
      "stnm": "某某流量站",
      "tm": "2026-07-29T17:00:00",
      "q": 2.452
    }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按监测时间倒序**，最新在前） |

**records[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| tm | String | 监测时间（ISO 格式） |
| q | Number | 流量 (m³/s) |

#### 数据来源

| 数据表 | 说明 |
|------|------|
| `t_auto_hltgq_water_wt_nfo` | 流量监测数据表 |
| `t_auto_hltgq_5nw74_vnqqef` | 站点信息表（`site = id`，取 `zzkaec` 为站名） |

---

### 19.4 日时段水情表（水位监测-花凉亭水库）

> 按选中日期 + 时段间隔 + 站点（多选）生成完整时间槽位，每个槽位填该整点的水位/水势/流量。
> 服务于水位监测-花凉亭水库-日时段水情表页面。

**请求方式**：`GET`

**请求路径**：`/flow-monitor/period-regime`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| date | **是** | `yyyy-MM-dd` | 选中日期（槽位从当日 08:00 起） |
| interval | 否 | int | 时段间隔（小时），1/2/3/6/12，默认 1 |
| stcds | **是** | String | 站点编号，多个逗号分隔（如 `320640000A,3206400007`） |

#### 响应格式

```json
[
  {
    "stcd": "320640000A",
    "stnm": "花凉亭坝下",
    "tm": "2026-08-14T08:00:00",
    "z": 3.25,
    "wptn": "涨",
    "q": 1.352,
    "msqmt": null,
    "msamt": null,
    "msvmt": null
  }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称 |
| tm | String | 槽位整点时间（ISO 格式） |
| z | Number | 水位 (m)，截断 2 位；该槽位无有效采集为 `null` |
| wptn | String | 水势：`涨` / `落` / `平` / `无涨落信息`（取槽位内最新记录） |
| q | Number | 流量 (m³/s)，截断 3 位（取槽位内最新记录的水位表 Q 字段） |
| msqmt | String | 测流方法，暂无数据源恒为 `null` |
| msamt | String | 测积方法，暂无数据源恒为 `null` |
| msvmt | String | 测速方法，暂无数据源恒为 `null` |

#### 计算规则

1. 槽位序列：选中日期 08:00 起，按 interval 递增到次日 07:00 止，**完整返回全部槽位（含无数据槽位）**
2. 槽位取值遵循业主整点口径：记录 tm ∈ (prevSlot, slot] 左开右闭 → 归属 slot，同槽位多条取最新；整点前的最后一条有效采集（Z 非空）作为该整点值，整点后的采集归下一整点，进入下一时段后该整点数值固定不变
3. 查询窗口为 [slotStart-interval, slotEnd]，Z 为空的采集记录不参与槽位匹配

#### 数据来源

| 数据表 | 说明 |
|------|------|
| `t_auto_hltgq_water_river_info` | 水位原始数据表（STCD/TM/Z/WPTN/Q） |
| `t_auto_hltgq_5nw74_vnqqef` | 站点信息表（按 STCD 查询，取 `zzkaec` 为站名） |

---

## 二十、墒情监测

> 数据来源：`t_auto_hltgq_water_nmisp_info`（site 站点 / device 设备 / stcd 测站编码 / tm 监测时间 / mten~mhundred 8 个深度含水量）。
>
> 全模块通用口径：
> - 含水量为 `-9991` 表示设备异常，后端**透传**，前端展示 `--`
> - 含水量为 `-999` 表示设备不存在，后端**透传**，前端不展示
> - 含水量为 `null` 表示无数据，前端展示 `-`
> - 站点标识 = `COALESCE(stcd, site)`：有测站编码用编码，无编码站点回退 site UUID（筛选参数两种都能传）

### 20.1 墒情监测-最新数据（首页）

> 每个站点返回最新一条记录，包含站点名称、站点编号、监测时间及 10/20/30/40/50/60/80/100 厘米含水量。支持按站点（多选）和监测日期筛选。

**请求方式**：`GET`

**请求路径**：`/soil-moisture/monitoring`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcds | 否 | String | 站点编号，多个逗号分隔。不传=全部墒情站点 |
| date | 否 | `yyyy-MM-dd` | 监测日期。选中日期过滤 `[date 00:00:00, date+1天 00:00:00)` 左闭右开区间 |

#### 调用示例

```
/hltgq-site/soil-moisture/monitoring
/hltgq-site/soil-moisture/monitoring?stcds=9000000101,9000000102&date=2026-08-16
```

#### 响应格式

```json
[
  {
    "stcd": "9000000101",
    "site": "9000000101",
    "stnm": "某某墒情站",
    "tm": "2026-08-16T17:30:00",
    "vol": 12.5,
    "mten": 23.45,
    "mtwenty": 24.12,
    "mthirty": 25.30,
    "mforty": 26.05,
    "mfifty": 26.80,
    "msixty": 27.42,
    "meighty": 28.10,
    "mhundred": 28.66
  }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 测站编码（无编码站点为 `null`，前端留空显示） |
| site | String | 站点标识（stcd 或 site UUID，无编号站点查询/筛选用，不用于展示） |
| stnm | String | 站点名称 |
| tm | String | 监测时间（ISO 格式） |
| vol | Number | 电压 (V)，取关联电压表 `t_auto_hltgq_water_vol_info` 在筛选时间范围内最新值，无数据为 `null`（**仅 monitoring 返回，history 不返回**） |
| mten | Number | 10 厘米土壤含水量 |
| mtwenty | Number | 20 厘米土壤含水量 |
| mthirty | Number | 30 厘米土壤含水量 |
| mforty | Number | 40 厘米土壤含水量 |
| mfifty | Number | 50 厘米土壤含水量 |
| msixty | Number | 60 厘米土壤含水量 |
| meighty | Number | 80 厘米土壤含水量 |
| mhundred | Number | 100 厘米土壤含水量 |

#### 查询逻辑

1. `DISTINCT ON (站点标识)` 每站点取 `tm` 最新的一条记录
2. 含水量截断 2 位小数；`-9991`/`-999` 透传，前端分别展示 `--`/不展示
3. 电压 vol 通过 `LEFT JOIN` 电压表 `t_auto_hltgq_water_vol_info`（`vol.site = 墒情表.site`，均为站点 UUID）取筛选时间范围内最新一条；无电压数据时 vol 为 `null`

---

### 20.2 墒情趋势

> 小时级含水量曲线（8 个深度各一条线，类似闸门开度趋势），默认近七天数据，支持按日期区间查询。

**请求方式**：`GET`

**请求路径**：`/soil-moisture/trend`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcd | **是** | String | 站点编号（或 site UUID） |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间，默认 7 天前整点 |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间，默认当前整点 |

#### 调用示例

```
/hltgq-site/soil-moisture/trend?stcd=9000000101
/hltgq-site/soil-moisture/trend?stcd=9000000101&startTime=2026-08-10+00:00:00&endTime=2026-08-17+00:00:00
```

#### 响应格式

```json
{
  "stcd": "9000000101",
  "stnm": "某某墒情站",
  "startTime": "2026-08-10T00:00:00",
  "endTime": "2026-08-17T00:00:00",
  "points": [
    { "hour": "2026-08-16 17:00", "mten": 23.45, "mtwenty": 24.12, "mthirty": 25.30, "mforty": 26.05, "mfifty": 26.80, "msixty": 27.42, "meighty": 28.10, "mhundred": 28.66 },
    { "hour": "2026-08-16 18:00", "mten": null,   "mtwenty": null,    "mthirty": null,    "mforty": null,   "mfifty": null,   "msixty": null,   "meighty": null,  "mhundred": null }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 站点编号 |
| stnm | String | 站点名称（站点无匹配数据时回退为 stcd） |
| startTime | String | 查询起始时间（ISO 格式） |
| endTime | String | 查询截止时间（ISO 格式） |
| points | Array | 小时级数据点（**严格 1h 步长，无缺失**） |

**points[].字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| hour | String | 小时标签 `yyyy-MM-dd HH:00` |
| mten ~ mhundred | Number | 该小时各深度平均含水量（2 位小数）。`-9991` 设备异常、`-999` 设备不存在不参与聚合；该小时无有效数据时为 `null`（前端图表断线） |

#### 计算规则

1. 查询原始墒情记录，按整点小时聚合：同一小时内每个深度字段取 `AVG`（`FILTER (WHERE 值 NOT IN (-999, -9991))` 排除设备异常与设备不存在）
2. 生成完整小时序列（从 startTime 到 endTime，步长固定 1h），无数据小时各深度字段为 `null`

---

### 20.3 墒情历史数据

> 分页返回指定站点的历史墒情记录（按监测时间倒序），支持按日期区间查询。

**请求方式**：`GET`

**请求路径**：`/soil-moisture/history`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| stcd | **是** | String | 站点编号（或 site UUID） |
| startTime | 否 | `yyyy-MM-dd HH:mm:ss` | 起始时间（含） |
| endTime | 否 | `yyyy-MM-dd HH:mm:ss` | 截止时间（含） |
| page | 否 | long | 页码，默认 1 |
| size | 否 | long | 每页条数，默认 10 |

#### 调用示例

```
/hltgq-site/soil-moisture/history?stcd=9000000101&page=1&size=10
/hltgq-site/soil-moisture/history?stcd=9000000101&startTime=2026-08-10+00:00:00&endTime=2026-08-17+00:00:00
```

#### 响应格式

```json
{
  "total": 150,
  "current": 1,
  "size": 10,
  "pages": 15,
  "records": [
    {
      "stcd": "9000000101",
      "site": "9000000101",
      "stnm": "某某墒情站",
      "tm": "2026-08-16T17:30:00",
      "mten": 23.45,
      "mtwenty": 24.12,
      "mthirty": 25.30,
      "mforty": 26.05,
      "mfifty": 26.80,
      "msixty": 27.42,
      "meighty": 28.10,
      "mhundred": 28.66
    },
    {
      "stcd": "9000000101",
      "site": "9000000101",
      "stnm": "某某墒情站",
      "tm": "2026-08-16T17:00:00",
      "mten": 23.40,
      "mtwenty": 24.08,
      "mthirty": 25.26,
      "mforty": 26.01,
      "mfifty": 26.76,
      "msixty": 27.38,
      "meighty": 28.06,
      "mhundred": 28.62
    }
  ]
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| total | long | 总记录数 |
| current | long | 当前页码 |
| size | long | 每页条数 |
| pages | long | 总页数 |
| records | Array | 数据列表（**按监测时间倒序，最新在前**） |

**records[].字段：** 与 [20.1 墒情监测-最新数据](#201-墒情监测-最新数据首页) 相同（stcd/site/stnm/tm/mten~mhundred，含水量截断 2 位，`-9991`/`-999` 透传）。

#### 与 /monitoring 的区别

| | `/monitoring` | `/history` |
|------|:--:|:--:|
| 每个站点 | 仅最新 1 条 | **全部记录** |
| stcd 参数 | 可选（多选筛选） | **必填**（单站） |
| 分页 | ❌ 全部返回 | ✅ 按 page/size 分页 |
| 使用场景 | 首页概览表格 | 历史数据表格 |

---

### 20.4 墒情站点列表

> 返回表中有墒情数据的站点（供下拉选择）。表中有数据即视为墒情站。

**请求方式**：`GET`

**请求路径**：`/soil-moisture/sites`

无参数。

#### 响应格式

```json
[
  { "code": "9000000101", "name": "某某墒情站" },
  { "code": "9000000102", "name": "另一墒情站" }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| code | String | 站点标识（COALESCE(stcd, site)） |
| name | String | 站点名称 |

#### 数据来源

| 数据表 | 说明 |
|------|------|
| `t_auto_hltgq_water_nmisp_info` | 墒情监测数据表（site/stcd/tm/mten~mhundred） |
| `t_auto_hltgq_5nw74_vnqqef` | 站点信息表（`site = id`，取 `zzkaec` 为站名） |

---

## 二十一、登录鉴权

> 供前端页面登录鉴权使用：校验 key/secret 后调用外部登录服务获取 sessionId。

**请求方式**：`POST`

**请求路径**：`/auth/login`

### 请求体（JSON）

| 字段 | 必填 | 类型 | 说明 |
|------|:--:|------|------|
| key | **是** | String | 接口调用密钥，需与服务端配置 `auth.api.key` 一致 |
| secret | **是** | String | 接口调用密钥，需与服务端配置 `auth.api.secret` 一致 |

```json
{
  "key": "xxx",
  "secret": "yyy"
}
```

### 响应格式

```json
{
  "success": true,
  "message": "登录成功",
  "sessionId": "abc123..."
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| success | Boolean | 是否登录成功 |
| message | String | 提示信息（失败原因见此处） |
| sessionId | String | 会话 ID（仅成功时有值，由外部登录服务返回） |

### 登录流程

1. 校验 key / secret 是否与服务端配置一致，不一致直接返回失败
2. 调用外部安全码接口（`qx-apaas-uc/user/securityImg`）获取 key，再调用外部登录接口（`qx-apaas-auth/server/auth/login`）获取 sessionId
3. 任一步骤异常均返回 `success=false` 及对应提示

---

## 二十二、闸站流量计算参数

> 闸站流量计算参数维护（表 `t_auto_hltgq_water_sluice_discharge`）。
> 每次修改**新增**一条数据（version 递增），回显与使用始终取该站点最新一条。

### 22.1 查询最新参数

**请求方式**：`GET`

**请求路径**：`/sluice-discharge/latest`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| siteId | **是** | String | 站点 UUID（对应站点表 id） |

#### 响应格式

```json
{
  "id": "uuid-xxx",
  "site": "uuid-site",
  "fullOpenFreeCoeff": 0.385,
  "submergedFlowCoeff": 0.350,
  "controlledFreeCoeff": 0.310,
  "orificeSubmergedCoeff": 0.600,
  "width": 5.00,
  "height": 4.50,
  "bottomElevation": 28.00,
  "version": 3,
  "createdAt": "2026-08-16T10:00:00",
  "updatedAt": "2026-08-16T10:00:00"
}
```

无数据时返回 `null`。

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 记录 UUID |
| site | String | 站点 UUID |
| fullOpenFreeCoeff | Number | 流量系数（全开自由流） |
| submergedFlowCoeff | Number | 流量系数（全开淹没流） |
| controlledFreeCoeff | Number | 流量系数（有闸控制自由流） |
| orificeSubmergedCoeff | Number | 流量系数（有闸控制淹没流） |
| width | Number | 孔宽 (m) |
| height | Number | 孔高 (m) |
| bottomElevation | Number | 闸底高程 (m) |
| version | Integer | 版本号，每次新增递增 |
| createdAt | String | 创建时间（ISO 格式） |
| updatedAt | String | 更新时间（ISO 格式） |

### 22.2 新增参数（version 自动 +1）

**请求方式**：`POST`

**请求路径**：`/sluice-discharge`

#### 请求体（JSON）

| 字段 | 必填 | 类型 | 说明 |
|------|:--:|------|------|
| site | **是** | String | 站点 UUID |
| fullOpenFreeCoeff | 否 | Number | 流量系数（全开自由流） |
| submergedFlowCoeff | 否 | Number | 流量系数（全开淹没流） |
| controlledFreeCoeff | 否 | Number | 流量系数（有闸控制自由流） |
| orificeSubmergedCoeff | 否 | Number | 流量系数（有闸控制淹没流） |
| width | 否 | Number | 孔宽 (m) |
| height | 否 | Number | 孔高 (m) |
| bottomElevation | 否 | Number | 闸底高程 (m) |

```json
{
  "site": "uuid-site",
  "fullOpenFreeCoeff": 0.385,
  "submergedFlowCoeff": 0.350,
  "controlledFreeCoeff": 0.310,
  "orificeSubmergedCoeff": 0.600,
  "width": 5.00,
  "height": 4.50,
  "bottomElevation": 28.00
}
```

#### 响应格式

返回保存后的完整记录（含新生成的 id、version、createdAt/updatedAt），字段同 [22.1 响应](#221-查询最新参数)。

#### 计算规则

1. 每次提交都**新增**一条记录（不覆盖旧数据），`version = 该站点当前最大 version + 1`（首条为 1）
2. **写保护**：配置 `sluice-discharge.write-enabled=false`（默认）时该接口返回 `403`（只读），防止参数被随意篡改；后续接入权限功能后可开启

---

## 二十三、站点信息维护

> 站点信息表（`t_auto_hltgq_5nw74_vnqqef`）维护接口。
> 主键 `iofhpi` = 测站编码（stcd），`id` = 站点 UUID（供闸门/流量/墒情等业务表 `site` 字段关联），`zzkaec` = 站点名称。

### 23.1 站点列表

**请求方式**：`GET`

**请求路径**：`/st-stinfo/list`

无参数。

#### 响应格式

```json
[
  { "stcd": "QSJSZ", "id": "uuid-xxx", "stnm": "渠首进水闸" },
  { "stcd": "SMH", "id": "uuid-yyy", "stnm": "双庙湖节制闸" }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| stcd | String | 测站编码（主键 iofhpi） |
| id | String | 站点 UUID（业务表 site 字段关联值） |
| stnm | String | 站点名称（zzkaec） |

### 23.2 站点分页

**请求方式**：`GET`

**请求路径**：`/st-stinfo/page`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| page | 否 | int | 页码，默认 1 |
| size | 否 | int | 每页条数，默认 20 |

响应为 MyBatis-Plus 分页结构（total/current/size/pages/records），records 元素同 [23.1 字段说明](#231-站点列表)，按测站编码升序。

### 23.3 站点详情

**请求方式**：`GET`

**请求路径**：`/st-stinfo/{stcd}`

路径参数 `stcd`：测站编码。返回单条站点记录（字段同 23.1），不存在返回 `null`。

### 23.4 新增 / 修改站点

**请求方式**：`POST`

**请求路径**：`/st-stinfo`

请求体为站点对象（JSON，字段同 23.1）。按主键 stcd 存在则更新、不存在则新增，返回 `true` / `false`。

### 23.5 修改站点

**请求方式**：`PUT`

**请求路径**：`/st-stinfo/{stcd}`

请求体为站点对象（JSON，字段同 23.1），更新指定测站编码的站点，返回 `true` / `false`。

### 23.6 删除站点

**请求方式**：`DELETE`

**请求路径**：`/st-stinfo/{stcd}`

删除指定测站编码的站点，返回 `true` / `false`。

---

## 二十四、设备状态概览（大屏统计）

> 大屏/首页统计卡片数据：设备在线率、闸门开启率、未处理告警数。

### 24.1 概览统计

**请求方式**：`GET`

**请求路径**：`/dashboard/overview`

无参数。

#### 响应格式

```json
{
  "totalDeviceCount": 42,
  "onlineDeviceCount": 38,
  "onlineDevicePercent": 90.5,
  "totalGateCount": 11,
  "openGateCount": 3,
  "openGatePercent": 27.3,
  "unhandledAlarmCount": 5
}
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| totalDeviceCount | long | 设备总数（设备表全部行） |
| onlineDeviceCount | long | 在线设备数（所属站点 zebpsu=`#1#` 在线的设备，站点状态由报文入库项目维护） |
| onlineDevicePercent | Number | 在线设备百分比（1 位小数 HALF_UP）；设备总数为 0 时为 `null` |
| totalGateCount | long | 闸门总数（近 24h 内有上报、排除站级行 `gate_no='0'` 与无信号/异常闸孔） |
| openGateCount | long | 开启闸门数（各闸孔最新开度 > 0） |
| openGatePercent | Number | 闸门开启百分比（1 位小数 HALF_UP）；闸门总数为 0 时为 `null` |
| unhandledAlarmCount | long | 未处理告警数（未关闭：`#1#` 未确认 / `#2#` 已确认 / `#3#` 处理中；`#4#` 已关闭不计） |

### 24.2 站点告警列表

**请求方式**：`GET`

**请求路径**：`/dashboard/alerts`

#### 请求参数

| 参数 | 必填 | 格式 | 说明 |
|------|:--:|------|------|
| site | **是** | String | 站点主键 ID（站点 UUID） |

#### 响应格式

```json
[
  {
    "id": "uuid-alert-1",
    "code": "AL-20260819-001",
    "site": "CAYQ739MiBWMg9gQvyi",
    "device": "uuid-device-1",
    "content": "设备离线",
    "level": "#2#",
    "status": "#1#",
    "time": "2026-08-19T10:30:00"
  }
]
```

#### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| code | String | 告警编号 |
| site | String | 告警站点（站点主键 ID） |
| device | String | 告警设备（设备主键 ID） |
| content | String | 告警内容 |
| level | String | 告警级别：`#1#` 一般、`#2#` 较重、`#3#` 严重、`#4#` 特别严重 |
| status | String | 处理状态：`#1#` 未确认、`#2#` 已确认、`#3#` 处理中、`#4#` 已关闭 |
| time | String | 发生时间（ISO 格式 `yyyy-MM-ddTHH:mm:ss`） |

#### 查询逻辑

1. 仅返回未关闭告警（`status IN ('#1#','#2#','#3#')`，`#4#` 已关闭不计），按发生时间倒序（最新在前）
2. 告警表列名 `level` 为 KingbaseES 层次查询伪列保留字，SQL 中需加双引号 `"level"` 转义（裸写报 `CONNECT BY clause required`）

---

## 附录：通用约定

| 项目 | 约定 |
|------|------|
| 前缀 | 生产环境统一加 `/hltgq-site`，如 `/hltgq-site/st-pptn-r/gq-rainfall` |
| 时间格式 | URL 参数用 `yyyy-MM-dd HH:mm:ss`（空格替换为 `+` 或 `%20`） |
| null | 表示数据不存在或无法计算，需前端自行处理为空值展示 |
| 0 | 表示该时段确实无变化或无降雨 |
| -9991 | 设备异常，后端透传，前端展示 `--`；聚合与增量计算需排除该值 |
| -999 | 设备不存在（报文原生），后端透传，前端不展示（如闸孔开度为 -999 则该闸孔不渲染）；聚合与增量计算需排除该值 |

---

## 附录：数据维护类基础接口

> 以下为雨量 / 水情**原始数据表**的 CRUD 与导出接口，面向数据维护场景（管理后台）。
> **业务前端请使用各业务接口**（如 `/st-pptn-r/gq-rainfall`、`/st-river-r/regime` 等）；这些接口返回的原始值可能与业务口径不同（如花凉亭水库报文 DRP 恒为 0，业务雨量请用 reservoir 系列接口）。

### 雨量原始数据（/st-pptn-r）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/st-pptn-r/latest` | 每站最新水文日聚合（原始 DRP 直接聚合，非业务雨量口径） |
| GET | `/st-pptn-r/list?stcd=` | 全部记录（可按 stcd 过滤），按 TM 升序 |
| GET | `/st-pptn-r/page?page=&size=&stcd=&startTime=&endTime=` | 分页查询，按 TM 升序 |
| GET | `/st-pptn-r/page-daily?page=&size=&stcd=&startTime=&endTime=` | 每日聚合分页（原始 DRP 直接聚合，非业务雨量口径） |
| GET | `/st-pptn-r/export?stcd=&startTime=&endTime=` | 全量导出（无分页），按 TM 升序 |
| GET | `/st-pptn-r?stcd=&tm=` | 单条查询（stcd + tm 联合主键） |
| POST | `/st-pptn-r` | 新增 / 修改（按 stcd+tm 存在则更新） |
| PUT | `/st-pptn-r` | 修改（按 stcd+tm） |
| DELETE | `/st-pptn-r?stcd=&tm=` | 删除（按 stcd+tm） |

### 河道水情原始数据（/st-river-r）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/st-river-r/latest` | 每站最新一条 |
| GET | `/st-river-r/list?stcd=` | 全部记录（可按 stcd 过滤），按 TM 升序 |
| GET | `/st-river-r/page?page=&size=&stcd=&startTime=&endTime=` | 分页查询，按 TM 升序 |
| GET | `/st-river-r/export?stcd=&startTime=&endTime=` | 全量导出（无分页），按 TM 升序 |
| GET | `/st-river-r?stcd=&tm=` | 单条查询（stcd + tm 联合主键） |
| POST | `/st-river-r` | 新增 / 修改（按 stcd+tm 存在则更新） |
| PUT | `/st-river-r` | 修改（按 stcd+tm） |
| DELETE | `/st-river-r?stcd=&tm=` | 删除（按 stcd+tm） |

> 时间参数格式均为 `yyyy-MM-dd HH:mm:ss`（URL 中空格替换为 `+` 或 `%20`）。
